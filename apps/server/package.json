{
  "name": "@selfalone/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "APP_ENV=development tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "latest",
    "@selfalone/contracts": "workspace:*",
    "@selfalone/domain": "workspace:*",
    "@selfalone/presentation-adapter": "workspace:*",
    "fastify": "latest",
    "postgres": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "tsx": "latest"
  }
}
