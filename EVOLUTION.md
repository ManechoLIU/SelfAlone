# SelfAlone 自我进化

## 1. 目的

本文件沉淀可复用的判断方法、反复错误和预防动作。它不定义产品或视觉要求，也不得覆盖 `redesign-v2/SPEC.md`、共享与端侧视觉事实源或当前原型。

只在以下情况更新：

- 用户指出错误或推翻方案；
- 真实验证推翻原判断；
- 同类问题重复出现；
- 形成能指导后续任务的方法。

普通进度、一次性细节、产品需求、视觉参数和未经验证的猜测不写入本文。

## 2. 判断方法

### 事实优先

- 区分事实、用户偏好、假设、建议和已接受决策。
- 时间敏感或外部能力必须用当前证据验证；无法验证时明确标记限制。
- 文件存在、测试通过、页面可打开和用户可见结果是不同层级的证据。

### 独立判断

- 不把用户提出的方向自动称为最佳方案。
- 重要选择给出替代方案、取舍、最强反方意见和推荐。
- 已接受决策直接沿用；只有证据或约束变化时才重新讨论。

### 先定义对象与状态

- 先明确对象、能力、来源、生命周期和失败影响，再设计页面入口。
- 草稿、任务和完成作品等不同生命周期不得因界面相似而混为一个对象。
- 外部连接需要覆盖未配置、验证中、失败、重试、失效和撤销。

### 保持上下游一致

- 产品规则、视觉规则、原型和测试各有职责，不维护第二套相同规则。
- 用户确认变化后立即做影响扫描和旧规则残留扫描。
- 历史只留在 Git，当前事实源只写有效结论。

### 用真实结果交付

- 先定义用户可观察的成功标准，再选择测试方式。
- 静态代码检查只能证明结构存在，不能证明交互顺畅或视觉正确。
- 报告必须区分已完成、已验证、未验证和受阻。

## 3. 常见错误与预防

| 错误 | 常见表现 | 预防动作 |
| --- | --- | --- |
| 过度顺从 | 用户提出方向后立即定案 | 先给替代方案、代价和反方风险 |
| 重复确认 | 已接受规则再次被当作开放问题 | 提问前读取当前事实源和本轮确认 |
| 被动维护文档 | 需求已变更，仍等待用户逐项提醒同步 | 变更结束前主动扫描相关事实源、原型和测试，并判断是否更新本文 |
| 过早确定 | 未验证的接口、权限或工具状态写成事实 | 标记假设并用真实环境验证 |
| 只画成功路径 | 遗漏未配置、失败、撤销和恢复 | 为每个数据面建立完整状态矩阵 |
| 产品与视觉混写 | 产品文档固定布局、组件或断点 | 产品写结果，设计写呈现，原型负责验证 |
| 桌面 Web 缩成小程序 | 多栏、操作组和密集卡片直接堆叠 | 从小程序主任务、触控、返回和安全区重新组织 |
| 生命周期混淆 | 草稿、生成任务和完成作品进入同一列表 | 先定义对象边界、进入条件和删除影响 |
| 只改可视稿 | 原型变化但事实源仍是旧规则 | 确认后同步文档、原型和测试并做残留扫描 |
| 静态测试代替体验 | 选择器或字符串测试通过就称交互完成 | 验证连续用户路径、状态恢复和真实视口 |
| 在旧结构上反复打补丁 | 共享文件持续膨胀、改一处影响多页 | 判断结构性重做成本，保留有效数据和资产而非盲目清零 |
| 文档持续膨胀 | 主文档混入历史、过程和重复解释 | 每个文件只承担一种职责，删除重复而非追加覆盖 |
| 根目录堆积产物 | 截图、导出、工具和生成文件散落 | 任务开始先定义输出目录，按版本和用途归档 |
| 误报交付 | 把旧日志、HTTP 200 或本地路径称为可见成果 | 检查当前产物、页面状态和用户可见边界 |

### E-20260818-01：区分 MVP 限制与长期对象模型

- 事实来源：用户明确说明 MVP 暂不支持跨书 PPT，但后续需要跨书 PPT，并会在会话中继续叠加其他任务能力。
- 问题：把“当前一次只选一本书”写成 PPT 任务的永久定义，又让会话结构围绕 PPT 能力展开。
- 根因：混淆了 MVP 的界面与校验限制、任务的长期资料模型以及会话的通用职责。
- 方法：会话只负责连续上下文与任务记录；任务使用可扩展的资料来源集合和任务类型；MVP 限制放在当前能力与提交校验中，不写成不可扩展的对象约束。
- 动作：新增任务能力或缩减 MVP 时，分别扫描对象定义、界面文案、提交校验、来源关系和恢复状态；避免用当前唯一能力命名通用容器，也不提前展示尚未实现的空泛入口。
- 状态：有效

### E-20260818-02：不要用更大的固定表单解决场景扩展

- 事实来源：用户否定为通用 PPT 扩展固定资料与范围表单，确认保留通用字段，并由对话按语义询问个性化范围。
- 问题：为了兼容未来资料类型，继续抽象或增加固定表单字段，反而让 MVP 更复杂，并把场景差异固化在页面结构中。
- 根因：把“能力通用性”误解为“所有场景共用一张更大的表单”，忽略对话本身可以动态收集结构化信息。
- 方法：固定界面只承载长期稳定的通用字段；场景化信息按需询问并使用可复用选择控件；另以轻量摘要保存当前有效状态，避免答案散落或出现双重事实源。
- 动作：新增字段前先判断它是跨场景不变量还是语义派生项；后者优先进入对话式采集，并验证已回答、已修改、过期和复杂选择的状态。
- 状态：有效

### E-20260819-01：流程确认不等于完整视觉定稿

- 事实来源：PPT 大纲、模板和生成中图片的流程结构曾获确认，但这些稿件缺少用户已定下的青瓷复古主体与左侧远山亭子；用户再次提供的截图与 `02-conversation-and-scope.png` 完全一致，并明确该图才是页面主体样式。
- 问题：把“流程可用”“图片已生成”“文件名更新”或“阶段齐全”误判为整张图片已经视觉定稿，导致缺失共享外壳的稿件进入完整定稿清单。
- 根因：参考索引只有单一“已确认”状态，没有区分完整视觉与局部流程 / 结构确认，整理时又用流程完整性代替逐层验收记录。
- 方法：参考稿至少区分“完整视觉定稿”和“已确认流程 / 结构”；后者只约束明确列出的局部内容，不能覆盖共享外壳、色彩和品牌资产。完整视觉定稿必须能对应到用户对具体图片的明确验收。
- 动作：整理开发参考前逐张核对验收记录、当前事实源和后续纠正，并为局部参考写出“仅可参考”范围；缺少完整视觉稿时明确记录缺口，不用旧稿补齐。
- 状态：有效

### E-20260819-02：局部简化不等于删除共享复古外壳

- 事实来源：用户要求阅读正文中的复古元素保持克制，但应用内定稿页面的左侧导航始终保留远山、树木和亭子；后续若只保留流程结构，容易把该共享外壳一并删掉。
- 问题：把“内容区装饰需要弱化”扩大解释为“整个页面不再使用复古元素”，破坏不同模块共享的视觉识别。
- 根因：没有区分内容画布中的主插画与导航边缘的低对比背景，也没有把共享外壳作为跨页面不变量单独核对。
- 方法：局部视觉反馈先确定作用区域；内容区可以简化，桌面 Web 左侧导航的低对比远山、树木与亭子继续以完整视觉定稿为准，并核对存在性、底部锚点、主体占比和与导航项的避让。
- 动作：生成页面修订稿前同时附上问题稿和完整视觉外壳参考；分别检查导航、页头、主画布与浮动角色，不能只看当前任务区。
- 状态：有效

### E-20260819-03：按语义工作区定义主题切换边界

- 事实来源：阅读候选稿的默认背景相较完整视觉定稿偏暖、偏红，且背景选择只影响局部正文；用户确认阅读背景应覆盖完整阅读工作区，但不改变左侧全局导航。
- 问题：只给正文换色会形成割裂的局部色块和接缝；让整个应用换色又会把阅读偏好误扩展为全局主题。
- 根因：按局部画布或页面 DOM 层级划定换色范围，没有按用户正在完成的阅读任务界定语义容器。
- 方法：视觉偏好以语义工作区为作用边界；阅读页的书籍信息、标签与工具、正文和全书进度统一适配，跨模块共享的应用外壳保持稳定，浮层再按当前明暗背景维持独立层级。
- 动作：验收每个阅读背景时，检查完整阅读工作区是否同步换色、全局导航是否保持应用配色，以及文字、分隔线、选区、焦点和浮层在明暗预设下是否清晰。
- 状态：有效

### E-20260822-01：平台名称不是响应式断点

- 事实来源：用户指出把桌面 Web 的窄窗口规则拿来审查微信小程序时，单列倾向和克制圆角被错误套用到小程序模板选择与主按钮。
- 问题：把桌面响应式宽度、手机屏幕和微信小程序交付端混成同一个概念，导致平台规则互相覆盖。
- 根因：共享规则与端侧规则写在同一文件中，又用宽度别名代替实际交付平台。
- 方法：`DESIGN.md` 只承载共享视觉和入口；桌面 Web 与微信小程序分别读取各自的视觉事实源。平台规则直接写平台名称；桌面 Web 需要响应式说明时写明确宽度，不用平台化的断点别名。
- 动作：视觉审查先确认交付端，再读取共享文档和唯一对应的端侧文档；变更后扫描旧平台术语。小程序“单任务”不等于所有内容都必须单列，模板和封面等适合比较的内容可以保持双列。
- 失效条件：交付端发生变化时，重新调整端侧文档结构与索引，不把新平台继续塞进现有文件。
- 状态：有效

### E-20260824-01：用分层外部记忆限制错误半径

- 事实来源：长任务中反复出现重复犯错、阻塞后停滞、脱离目标、遗忘和记错；当时名为 `PROJECT_STATUS.md` 的旧台账曾同时承载当前进度、产品决定与视觉历史，恢复时难以定位明确下一动作。
- 问题：把当前上下文、交接摘要、知识、长期记忆和任务台账混成一份不断追加的文档，会产生重复事实源；工作包过粗又让隐含决定扩散到整个里程碑。
- 根因：没有按信息寿命和控制职责分层，也没有让台账粒度随探索、建设和验收阶段变化。
- 方法：当前上下文与 Compact 只负责临时续接，Wiki 负责检索，`MEMORY.md` 只保存带证据的稳定结论，`TASK_LEDGER.md` 只控制执行；探索和高风险边界拆细，建设使用功能级，验收再切到场景级。
- 动作：每个 Goal 绑定一个可验收台账项；完成后回写状态、实际证据、风险和下一步。发现大范围改动、长时间无证据、反复换方案或大返工时立即拆细，机械项没有独立验收价值时合并。
- 失效条件：项目改用能同时保证唯一事实源、可审计证据和中断恢复的其他控制机制时重新评估。
- 状态：有效

### E-20260824-02：分支隔离不等于环境隔离

- 事实来源：`main` 尚停在 M0 时，多个 detached worktree 已有阅读、邮箱认证、模型、小程序和 Presenton 候选提交；它们共用的本地 PostgreSQL 已出现 M1 账户结构和数据，导致按纯 M0 假设执行的迁移在账户外键处失败。
- 问题：只检查当前分支会把已有候选误判为未开发；只检查共享数据库又会把其他分支的副作用误判为当前代码能力，最终同时产生重复实现和错误基线。
- 根因：Git 工作树隔离了文件，却没有自动隔离数据库、端口、产物目录和后台进程；台账也没有登记未合并候选及其环境副作用。
- 方法：工作包启动门禁同时核对当前 HEAD / diff、全部 worktree、未合并提交和共享环境；代码完成状态只由目标候选提交与验证证据决定，数据库状态只作为待对账证据。
- 动作：并行工作默认使用独立 schema、端口和产物目录；发现重叠候选先登记并审查，选择一个主候选后再补缺口，禁止从头再做。迁移必须保留未知但有效的 owner，遇到无法证明归属的数据则失败关闭。
- 状态：有效

## 4. 工作检查

### 开始前

1. 用一句话写清目标、成功标准和不做事项。
2. 核对当前事实源、指定资料、工作目录和未提交改动。
3. 区分已接受决策、待验证假设和本次建议。
4. 对高成本、外部或不可逆操作确认权限和回退方式。

### 交付前

1. 检查用户真正关心的结果，而不只检查完成动作。
2. 检查正常、加载、空、失败和恢复状态。
3. 检查事实源、原型、测试和路径是否一致。
4. 说明未验证内容、剩余限制和未做事项。
5. 判断本次是否产生需要追加到本文的新经验。

## 5. 记录模板

新增复盘使用以下最小结构：

```markdown
### E-YYYYMMDD-NN：标题

- 事实来源：用户反馈、测试、真实页面或代码证据
- 问题：可观察到的错误
- 根因：为什么发生
- 方法：可复用的判断原则
- 动作：以后如何预防或验证
- 状态：有效 / 被替代
```

每条记录必须有真实事实来源、抽象方法和可执行动作；不能只写“下次注意”。敏感信息、凭证和无关项目细节不得记录。
