DATABASE_URL=postgres://selfalone:selfalone@127.0.0.1:55432/selfalone
PORT=4100
APP_ENV=development
# Explicit local H3 fake only; leave unset to keep conversation replies fail-closed.
# CONVERSATION_RESPONDER_MODE=development
ARTIFACT_DIR=./data/artifacts
BOOK_OBJECT_DIR=./data/artifacts/books
