DATABASE_URL=postgres://selfalone:selfalone@127.0.0.1:55432/selfalone
PORT=4100
APP_ENV=development
# Server-owned DeepSeek catalog: https://api.deepseek.com + deepseek-v4-flash.
# Chat uses POST /chat/completions, thinking.type=disabled, max_tokens=128,
# stream=false. Enter credentials only through authenticated model settings;
# never put a provider key in this file.
# Explicit local H3 fake only; leave unset to use the account-scoped provider
# adapter (which fails closed when no verified account credential exists).
# CONVERSATION_RESPONDER_MODE=development
# MODEL_CONFIG_VALIDATOR_MODE=fake
ARTIFACT_DIR=./data/artifacts
BOOK_OBJECT_DIR=./data/artifacts/books
