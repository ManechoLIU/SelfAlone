---
name: selfalone-delivery
description: Use when planning, implementing, reviewing, documenting, or reporting work in the SelfAlone project.
---

# SelfAlone delivery style

## 核心结果

交付必须形成用户能看到、能操作、能恢复、能验证的真实闭环。功能、持久化、完整状态、既定视觉、真实端侧体验和必要外部验收缺一项时，不得用代码量、骨架或测试数量替代。

## 输出风格与结构

- 默认中文，先说结果，再说证据、真实边界和下一动作。
- 保持简洁，格式只为帮助判断；不要堆叠标题、流程口号或重复背景。
- 不盲从用户判断。给出证据、最强反面理由、取舍和明确推荐。
- 只有缺失选择会实质改变结果、风险或授权边界时才暂停确认。
- 比较方案时使用“方案 / 代价 / 风险 / 推荐”；交付时使用“完成 / 验证 / 未覆盖 / 下一步”。

## 禁用表达与行为

- 没有当前证据时不得说“已完成”“已修复”“已通过”“已经支持”。
- 不使用“先搭框架，以后再补 UI”作为用户可见功能的完成路径。
- 不把静态页面、fixture、测试数量、文件存在、Agent 报告、参考图或共享数据库现状冒充当前分支能力。
- 不用“大概、应该、看起来”包装未经核验的当前事实；直接标为未验证并说明核验方法。
- 不创建第二份任务台账、会话交接文档或重复事实源。

## 判断方式

1. 用户最新明确要求优先；其后按 `AGENTS.md` 的事实源顺序处理。
2. 产品功能与视觉不是两个收尾阶段。实现 Web 或小程序时，同时读取 `SPEC.md`、共享视觉、目标端规范和已认可参考图。
3. 里程碑和模块只作为父级分组；可执行项拆到一个能独立交付、失败、回退和验收的结果。用户可见项把行为、持久化、完整状态、目标端视觉和真实端验收绑在一起；高风险、探索和验收再按问题或场景拆细，低风险机械步骤没有独立验收价值时合并。
4. 多 Agent 只用于无顺序依赖、文件所有权互斥且环境边界明确的工作包；共享文件顺序写入。
5. 优先最小真实纵切；不建设当前验收不需要的通用框架。

## 验收方式

- 核对产品行为、数据持久化、加载 / 空 / 失败 / 正常等完整状态、视觉还原和可访问性。
- Web 用真实浏览器验收；小程序用开发者工具并在阶段门要求时进行真机验收；PPTX 在授权门内用 PowerPoint / WPS 打开、编辑和重存。
- 外部服务、费用、认证、迁移和发布按 `AGENTS.md` 的授权与回滚门执行。
- 完成前运行风险相称的定向测试、相关回归、真实业务 Case、diff 审查和格式检查；范围单一且通过后可建本地提交。

## 落档规则

| 内容 | 唯一位置 |
| --- | --- |
| 项目背景、技术栈、目录、命令、禁区、长期约定 | `AGENTS.md` |
| 当前目标、任务、状态、阻塞、下一步、验收、证据 | `TASK_LEDGER.md` |
| 知识目录、原始资料与收录流程 | `WIKI_INDEX.md` |
| 稳定偏好、项目事实、关键决定、坑点与复用流程 | `MEMORY.md` |
| 产品 / 视觉 / 技术规则 | `redesign-v2/SPEC.md`、`DESIGN*.md`、`TECHNICAL.md` |
| 可复用工程方法 | `EVOLUTION.md` |

讨论过程、临时猜测、敏感信息和一次性任务细节不落入长期文档。只更新真正受影响的事实源，并用链接代替复制正文。
