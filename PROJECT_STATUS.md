# 项目状态

> 这是唯一的当前状态账本。只保留并替换当前事实，不追加会话历史；产品、视觉和技术细节只链接到对应权威文档。

## 当前目标与里程碑

- 当前开发版本为 `redesign-v2`。
- 桌面 Web 核心页面参考图已经完成收口；移动端交付形态已调整为微信小程序，现有 Compact 会话抽屉、输入框桌宠与 `390×844` 对话主画面候选继续沿用，等待用户审阅后再决定是否升级为参考图；技术设计与当前原型尚未开始。
- 下一可验收里程碑为用户确认或修订微信小程序对话主画面候选；进入技术设计或当前原型仍需用户另行确认。

## 已确认决定

- 产品目标、行为、流程、状态和验收以 [`redesign-v2/SPEC.md`](redesign-v2/SPEC.md) 为准。
- 视觉、布局、组件、响应式、动效和视觉验收以 [`redesign-v2/DESIGN.md`](redesign-v2/DESIGN.md) 为准。
- 设计参考图的确认层级、适用范围和原稿链接只查 [`redesign-v2/design-reference/README.md`](redesign-v2/design-reference/README.md)。
- 客户端形态为桌面 Web 与微信小程序，两端共享产品核心闭环；原移动端 Web 不再是 MVP 必交付端。
- 桌面 Web 以微信登录为主、邮箱为备选，微信小程序使用微信登录；微信与已有邮箱账户的绑定完全可选，绑定后进入同一账户并共享数据，未绑定时两个账户与数据保持独立，手机号不进入 MVP。
- 书籍详情“PPT 作品”页面已登记为认可页面参考；它约束作品网格和状态层级，不覆盖共享外壳、冷调背景及原始头像 / 桌宠资产规则。
- 书籍详情“老己笔记”页面已登记为认可页面参考，其单列全宽列表节奏同时用于“划线与想法”；两个标签均不继承标题式首行，且不照搬示例内容、偏暖背景或重绘角色。
- 移动端会话抽屉打开态已登记为认可页面参考；它约束 Compact 抽屉的层级、留白、大圆角、无卡片会话列表与下部远山亭子视觉落款，但不提供运行时头像、图标、会话内容或整图素材。
- 移动端对话输入框桌宠采用微歪头、双臂趴靠上沿并双手扶小书的探头式造型；认可图只锁定视觉，不是运行时资产，其棋盘格背景不得进入实现。
- `v1/` 只读且不默认继承；进入技术设计后再创建 `redesign-v2/TECHNICAL.md`。

## 进行中工作包

| 工作包 | 负责人 | 文件或范围 | 状态 |
| --- | --- | --- | --- |
| 微信小程序对话主画面视觉收口 | 主 Agent | `redesign-v2/work/mobile-design/`、`redesign-v2/design-reference/` 与对应视觉事实源 | 现有 Compact 主画面候选与交互继续沿用；已完成三档视口检查，待用户审阅，尚未升级为参考图 |

## 最近验证证据

- 2026-08-19：已对账仓库根目录、当前 `HEAD` 和未提交改动；现有改动均按用户工作保留。
- 2026-08-19：初始化档案审计确认仅缺本状态账本；根级 `SPEC.md`、`DESIGN.md` 和 `TECHNICAL.md` 会形成平行事实源，因此未创建。
- 2026-08-19：已锁定并登记书籍详情“PPT 作品”参考图，原稿与开发参考副本均为 `1487×1058`，SHA-256 为 `5944dffa44247538b561381e1f2775ff3a220d59ea3fc1b40abb9ac03f4b2632`。
- 2026-08-19：已锁定并登记书籍详情“老己笔记”参考图，原稿与开发参考副本均为 `1486×1058`，SHA-256 为 `b27d3863033fea8fe39e03d971a44e2b8d672fc2fdc39dfb70e9827dc6eadbdf`；桌面端参考图阶段据此收口。
- 2026-08-19：已锁定并登记移动端会话抽屉打开态参考图，原稿与开发参考副本均为 `853×1844`，SHA-256 为 `9e764e60189845b6f46eb1181c97bbc622a363c067274ae479cfb7a15c68c36e`。
- 2026-08-19：已锁定并登记 Compact 对话输入框趴卧桌宠造型，原稿与开发参考副本均为 `1254×1254`，SHA-256 为 `c8c7dac388c2e1437db2f483b7e83837b58020e80932516cf22405a288457721`；检测结果为无透明通道，故仅作为视觉元素参考。
- 2026-08-20：已输出移动端对话主画面候选 `redesign-v2/work/mobile-design/conversation-shell-v1/conversation-shell-390x844.png`，并在真实浏览器验证选择确认、抽屉开关和输入发送；`360×800`、`390×844`、`430×932` 均无横向溢出，桌宠不与输入文字区或发送按钮相交。
- 2026-08-20：按授权仅执行一次桌宠背景提取 ImageGen 编辑且未自动重试；输出仍为 `1254×1254` RGB PNG、没有透明通道，因此只可用于当前视觉探索稿的临时合成，不得升级为运行时资产。

## 阻塞

- 当前无外部阻塞。趴卧桌宠的两次候选均缺少透明通道；这不阻塞用户审阅移动端主画面候选，但在原型或实现接入前必须取得并确认透明运行时资产。

## 下一步

- 由用户按微信小程序 Compact 体验审阅现有对话主画面候选；确认后才复制到 `design-reference/` 并同步视觉事实源，需修订则继续留在 `work/mobile-design/`。透明桌宠资产在进入原型或实现前单独处理与确认。默认不重做桌面登录参考图、不扩展其他桌面端参考图，也不在确认前创建 `redesign-v2/TECHNICAL.md` 或当前原型。
