# SelfAlone 当前任务台账

> 这是唯一执行控制面，只保存当前工作包、依赖、状态、验收、证据、阻塞和恢复动作。产品、视觉和技术规则只链接到权威文档；完成历史由 Git 保存。

## 使用规则

- 状态：`PENDING` 依赖未满足，`READY` 可开始，`ACTIVE` 正在执行，`VERIFY` 等待验收收口，`BLOCKED` 无法继续，`DONE` 已有完整证据，`SUPERSEDED` 已被替代。
- 同一时刻最多一个 `ACTIVE` 工作包。开始、完成、阻塞或拆分工作包时必须同步本文件。
- 一个开发工作包按“开发 → 定向与相关回归 → 真实业务 Case → 修复与优化 → 复验 → diff 审查 → 本地 Commit → 台账回写”闭环；远端 push、合并、PR、发布和部署另行授权。
- 界面功能使用真实浏览器或 Computer Use 拟真验收；后端、迁移和文档使用与风险相称的集成、恢复或链接证据。Agent 报告本身不算证据。

## 当前指针

- 当前版本：`redesign-v2`
- 当前集成基线：`main` @ `2b639e0`；本台账修改位于同提交的 detached 任务工作树
- 当前活动项：无
- 唯一下一项：`M1-F2 真实文件与阅读笔记`
- 产品事实：[`redesign-v2/SPEC.md`](redesign-v2/SPEC.md)
- 技术事实：[`redesign-v2/TECHNICAL.md`](redesign-v2/TECHNICAL.md)
- 里程碑方案：[`docs/superpowers/plans/2026-08-23-mvp-launch.md`](docs/superpowers/plans/2026-08-23-mvp-launch.md)

## 里程碑

| 里程碑 | 状态 | 可观察结果 | 下一门禁 |
| --- | --- | --- | --- |
| M0 可连续点击的桌面闭环 | `DONE` | PostgreSQL 中完成开发数据的 PPT 四阶段、刷新恢复和本地 PPTX 下载 | 不把开发适配器或种子账户冒充真实用户闭环 |
| M1 真实桌面内测闭环 | `ACTIVE` | 一位邀请用户通过邮箱账户完成真实文件、阅读笔记、模型与 PPTX 闭环 | 按 `M1-F2 → F1 → F3 → F4 → F5 → V1` 单线集成 |
| M2 国内双端候选 | `PENDING` | 微信登录、微信读书和小程序复用同一账户及后端 | M1 完成且微信外部验证获授权 |
| M3 邀请制上线 | `PENDING` | 国内服务器与小程序候选可回滚地向受邀用户开放 | M2、适用外部验收及上线授权完成 |

## 工作包队列

| ID | 粒度 / 模式 | 状态 | 目标 | 依赖 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| GOV-01 | 功能级 / 治理 | `DONE` | 建立五层上下文职责、项目记忆、知识索引、Goal 循环与可恢复台账 | M0 | 以 `M1-E0` 试运行 |
| M1-E0 | 场景级 / 探索 | `DONE` | 对账账户归属、M0 无账户数据和安全迁移路径 | GOV-01 | 进入 `M1-R0` |
| M1-R0 | 场景级 / 探索 | `DONE` | 对账未合并 M1 候选链，确定唯一集成顺序与废弃项 | M1-E0 | 进入 `M1-F2` |
| M1-F1 | 功能级 / 建设 | `PENDING` | 邮箱注册、登录、找回密码和服务端会话 | M1-F2 | 只承接 `658ba50` 的直接功能差异并对齐 M1-E0 |
| M1-F2 | 功能级 / 建设 | `READY` | 真实 EPUB/TXT/文本 PDF 导入、阅读位置、划线、想法和老己笔记 | M1-R0 | 以 `67d77d7` 为唯一功能候选，按当前事实源重放 |
| M1-F3 | 功能级 / 建设 | `PENDING` | 四家固定文本模型配置、加密保存、检测与调用 | M1-F1 | 只承接 `3e4b020` 的直接功能差异，不带入小程序父链 |
| M1-F4 | 功能级 / 建设 | `PENDING` | 免费体验领取、成本账本与 `¥5` 事务硬上限 | M1-F1、M1-F3 | 等待前置项 |
| M1-F5 | 功能级 / 建设 | `PENDING` | 固定版本 Presenton、三套模板、停止、失败和恢复 | M1-F4 | 以 `39b84a4` 为唯一适配器种子，补齐已确认缺口 |
| M1-V1 | 场景级 / 验收 | `PENDING` | 邮箱登录至真实 PPTX 下载的完整 Case 与残留修复 | M1-F1–F5 | 等待前置项及真实调用授权 |

执行中若某项触发高风险或出现长时间无证据、大范围改动、反复换方案，先拆成更小场景项；低风险机械子项只有独立验收价值时才进入台账。

### M1-R0 候选结论

候选对象均可读取，但都从 `f85299b` 与当前 M1-E0 基线分叉；以下选择只认实际直接差异，不把提交标题、旧台账或共享数据库现状当作 `main` 功能：

| 工作包 | 唯一结论 | 直接证据与边界 |
| --- | --- | --- |
| M1-F2 | 主候选 `67d77d7` | 直接改动 14 个领域、服务端、Web、契约和测试文件；保留真实导入 / 阅读 / 标注 / 笔记实现。后续 `8688b6f` 改写当前 `DESIGN.md` token、登记当前事实源没有的运行时资产并携带旧台账，因此只作视觉实现证据，不直接集成。 |
| M1-F1 | 主候选 `658ba50` | 直接改动 25 个认证、服务端、Web、领域和 workspace 文件；与 M1-E0 的 `app.test.ts / m0-runtime.ts / domain index` 重叠，必须在 F2 后按 M1-E0 账户归属重新落位，不能带入其 F2 祖先。 |
| M1-F3 | 主候选 `3e4b020` | 直接改动 17 个模型配置、API、Web、领域和测试文件；其父链夹有 M2 小程序提交 `3a4ed35`，集成时只取直接功能差异，真实供应商验证仍停在 H4 授权门。 |
| M1-F5 | 主候选 `39b84a4` | 与 `d5ee177` 的 3 个文件 blob 和稳定 patch-id 完全相同；选择位于干净 `146b` 工作树的 `39b84a4`，`d5ee177` 标记 `SUPERSEDED`。候选只完成固定模板映射和 fail-closed API，`stop()` 仍明确返回不支持，固定镜像、真实模板、停止、重启恢复和 PowerPoint/WPS 验收仍需在 F5 完成。 |

`42d85fe → 925bbc1` 保留为 M2 只读证据，不进入 M1：`925bbc1` 还把当前 `SPEC.md` 的纵向分页改为连续滚动，属于产品事实漂移，未经重新确认不得吸收；其 `6f5c` 工作树未提交内容也不属于本次候选。

### GOV-01 完成证据

- 职责与恢复规则：[`AGENTS.md`](AGENTS.md)；项目记忆：[`MEMORY.md`](MEMORY.md)；知识入口：[`WIKI_INDEX.md`](WIKI_INDEX.md)。
- 当前台账已移除产品决定和视觉验收历史，只保留 M0–M3、M1 工作包、唯一下一项和 `M1-E0` 的完整恢复字段。
- 2026-08-24 验证：检查本次治理文档中的 `48` 个本地链接，失效链接为 `0`；文档差异格式检查通过；恢复检查能唯一定位 `M1-E0`，并确认目标、非目标、依赖、验收、暂停条件、证据和恢复动作齐全。
- 边界：本项没有修改运行代码、产品行为、视觉规则或远端状态。

### M1-E0 完成证据

- 领域规则：`packages/domain/src/account-identity.ts` 覆盖新身份独立账户、重新验证、退役空账户、拒绝两个有数据账户合并和重复绑定无副作用。
- 数据迁移：`apps/server/src/account-migration.ts` 在单事务中创建账户 / 身份基础、保留已有 owner、回填六张 M0 表、建立 `NOT NULL`、账户外键、父子同账户外键和账户级任务幂等索引；重复执行稳定，后段约束失败时整笔回滚。
- 运行边界：M0 种子、查询、更新、任务、页面和产物均显式限定 `account-development-local`，不再依赖无归属的全局数据。
- 自动验证：2026-08-24 运行 `pnpm verify`，类型检查、7 个测试文件的 23 个断言和生产构建通过；身份外部键唯一约束完成红灯突变与恢复复验。
- 真实 PostgreSQL：迁移连续初始化两次成功；迁移前后仍为书籍 `10`、会话 `3`、草稿 `3`、任务 `1`、页面 `3`、产物 `1`，空 owner 为 `0`；保留原 3 个 UUID 账户并只补建缺失的开发账户，11 条账户 / 同账户父子约束和 `(account_id, idempotency_key)` 唯一索引存在。
- 真实边界：本项是后端与迁移工作，没有新增用户界面，因此不做形式化 Computer Use；邮箱 / 微信认证、数据账户合并、外部调用和候选分支集成仍未完成。

### M1-R0 完成证据

提交依赖图（箭头右侧提交依赖左侧提交；当前 `main` 不是任何候选的后代）：

```text
f85299b → 67d77d7 → 519801f → edcd8cf → 8688b6f
                                              ├→ 42d85fe → 925bbc1
                                              └→ 658ba50 → 96498c9 → 3a4ed35 → 3e4b020
                                                                                  ├→ c4b42eb → 39b84a4
                                                                                  └→ 2f79bda → d5ee177
f85299b → … → 2b639e0 (current main，含 M1-E0)
```

直接提交文件重叠矩阵；数字为共同路径数，括号列出需要人工重排的主要接缝：

| 组合 | 重叠 | 结论 |
| --- | ---: | --- |
| M1-E0 × `67d77d7` | 2 | `PROJECT_STATUS.md`、`packages/domain/src/index.ts`；旧状态不能覆盖，阅读必须接入 E0 账户归属。 |
| M1-E0 × `658ba50` | 3 | `app.test.ts`、`m0-runtime.ts`、domain index；认证 schema / owner 只能以 E0 为准。 |
| M1-E0 × `3e4b020` | 2 | `PROJECT_STATUS.md`、domain index；旧状态排除，凭据外键对齐当前账户。 |
| `67d77d7` × `658ba50` | 8 | server app/index/reading runtime、Web main/styles、contracts/domain index；必须 F2 先形成账户化运行时。 |
| `67d77d7` × `3e4b020` | 6 | 旧台账、server app/index、Web main、contracts/domain index；按工作包顺序重排入口。 |
| `658ba50` × `3e4b020` | 7 | server app/index、Web api/main、contracts/domain index；F3 只能落在冻结后的 F1 API / session 上。 |
| `39b84a4` × `d5ee177` | 3 / 3 | 三文件和整棵 tree 完全相同；`d5ee177` 为 `SUPERSEDED`。 |
| `42d85fe` × `925bbc1` | 9 | `925bbc1` 是严格后继且改变阅读行为；两者只保留为 M2 证据。 |

事实源与实现漂移：

- 所有候选中的旧 `PROJECT_STATUS.md` 均被当前台账取代，不能随功能差异进入集成。
- `67d77d7` 仍硬编码开发账户，图片型 PDF 直接拒绝且不保留失败文件；须按当前 E0 账户边界和 `TECHNICAL.md` 的失败记录规则适配，不能原样落库。
- `8688b6f` 改写共享视觉 token 并登记当前视觉事实源没有的运行时资产；不因历史截图或旧状态自动恢复，只允许按当前 `DESIGN*` 重新验收其代码价值。
- `658ba50` 是 Web 邮箱认证候选，不包含微信、小程序不透明令牌、邮箱修改 / 验证和账户绑定；本轮只覆盖 F1 已定义的邮箱链，其他能力不扩入。
- `3e4b020` 没有真实模型调用、成本账本或 Worker 链，也没有实现当前 `TECHNICAL.md` 要求的千问北京业务空间端点目录与真实供应商验证；配置保存可复用，目录与真实验证必须按当前事实补齐。
- `39b84a4` 只允许 localhost，未实现 worker/API 接缝、停止、重启恢复、artifact 安全归档、固定镜像或真实 PowerPoint/WPS；当前技术允许独立内网服务，F5 必须重新收敛部署边界。
- `925bbc1` 把当前 `SPEC.md` 的分页阅读改为连续滚动；该产品变更未在当前事实源生效，M1 不吸收。

工作树与共享环境快照：

| 工作树 | HEAD / 状态 | 本次结论 |
| --- | --- | --- |
| 原主工作区 | `main` @ `2b639e0`；340 个 `v1/` 删除、3 个未跟踪 smoke PPTX | 他人现场，只读，不进入任何提交。 |
| `0204` | `9783628`；4 个修改、34 个未跟踪路径 | 他人未提交现场，不作为候选。 |
| `146b` | `39b84a4`；干净 | F5 规范候选验证树。 |
| `3175` | `2b639e0`；仅本台账 | M1-R0 唯一写入位置。 |
| `60b5` / `f509` | `f85299b`；干净 | 旧共同基线。 |
| `6757` | `519801f`；干净 | F2 父链只读证据。 |
| `6f5c` | `925bbc1`；9 个修改、10 个未跟踪路径 | M2 他人现场，不验证、不吸收。 |
| `a076` | `d5ee177`；4 个修改、1 个未跟踪测试 | 他人现场；提交本身已被 `39b84a4` 替代。 |

两次短间隔快照中 9 个 worktree 的 HEAD 与状态哈希均未变化；这只证明观察窗口稳定，任何集成批次开始前仍须重取快照。共享 PostgreSQL 17.11 的 `public` 已有 16 张表，其中 `book_files / book_sections / reading_positions / highlights / notes`、`sessions / email_tokens`、`model_credentials` 分别对应 F2、F1、F3 的结构痕迹；当前计数包含 `sessions=8`、`email_tokens=3`、`model_credentials=1`，且没有迁移登记表。另有来源无法确认的 `selfalone_visual_empty_qa` schema。共享库现状已漂移，只是副作用证据，不是 `main` 功能或候选验收；后续批次必须使用隔离 schema 或独立实例。

候选验证在干净 `146b` @ `39b84a4` 执行：`pnpm verify` 的全仓类型检查、19 个测试文件 114 项测试和生产构建通过；按候选自带测试路径定向复跑 F2 为 2 文件 5 项、F1 为 5 文件 30 项、F3 为 4 文件 24 项、F5 为 1 文件 17 项，全部通过。它证明旧候选组合的自动回归，不证明与 `2b639e0` 集成，也不替代真实浏览器、真实文件、外部模型或 PowerPoint/WPS 验收。

唯一集成与回退顺序：

| 批次 | 范围与真实验收 | 失败回退点 / 授权点 |
| --- | --- | --- |
| 0 | 从 `2b639e0` 新建隔离集成工作树和独立数据库 schema，重新冻结全部 worktree；不触碰原主工作区。 | 快照变化或归属冲突即停；创建集成分支和本地提交可在工作包内执行，合入 `main` 需用户另行授权。 |
| 1 `M1-F2` | 重放 `67d77d7` 的有效功能差异；真实 EPUB/TXT/文本 PDF 与图片 PDF 失败、账户隔离、刷新恢复、划线 / 想法 / 笔记 CRUD，并按当前视觉在真实浏览器验收。 | 回退到 `2b639e0` 或丢弃隔离分支；不得带入旧台账、`8688b6f` 的事实源改写或共享库数据。 |
| 2 `M1-F1` | 重放 `658ba50` 直接差异；真实 PostgreSQL 完成注册 / 登录 / 登出 / 找回 / 重置、Cookie 失效、账户隔离和浏览器刷新恢复。 | 回退到 F2 本地提交；真实邮件发送需单独授权，未授权只验本地 outbox / adapter。 |
| 3 `M1-F3` | 重放 `3e4b020` 直接差异；四家当前目录、AES-GCM 密文、检测成功后替换、失败保留旧配置、并发和撤销恢复。 | 回退到 F1 本地提交；真实供应商、模型、次数和预算确认后才进入 H4。 |
| 4 `M1-F4` | 新实现成本账本与 `¥5` 事务硬上限，覆盖并发、幂等和失败结算。 | 回退到 F3 本地提交；未获费用授权不执行付费验证。 |
| 5 `M1-F5` | 以 `39b84a4` 为适配器种子，完成固定镜像、三模板、worker/API、停止 / 失败 / 重启恢复和安全产物，再用真实 PowerPoint / WPS 打开重存。 | 回退到 F4 本地提交；Presenton 服务与真实导出范围需单独确认，`d5ee177` 不再进入集成。 |
| 6 `M1-V1` | 在同一候选快照跑邮箱登录到真实 PPTX 下载 H3，并关闭残留。 | 通过后才请求合并 / push；push、PR、发布和部署始终另行授权。 |

### M1-R0 真实边界

- 本项没有修改或修复候选实现，没有 cherry-pick、merge、push、PR、发布、部署或数据库写入。
- `0204 / 6f5c / a076` 与原主工作区内容均保持原样；其未提交差异不是本次候选的一部分。
- 主候选表示“下一工作包的唯一取证起点”，不表示该功能已在 `main` 完成；每批只有在集成快照完成对应真实验收后才可标记 `DONE`。

## 当前可执行项：M1-F2

### 目标

在 `2b639e0` 的 M1-E0 账户归属基线上，只承接 `67d77d7` 的有效功能差异，完成账户级真实 EPUB/TXT/文本 PDF 导入、阅读位置、划线、想法和老己笔记闭环。

### 非目标

- 不吸收 `8688b6f` 的旧台账、共享视觉 token 或未在当前事实源确认的运行时资产。
- 不实现邮箱认证、模型、免费额度、Presenton 或小程序；不修改其他 worktree。
- 不使用共享 `public` schema 验收，不把旧候选数据库现状当作本批结果。

### 依赖与范围

- 依赖：`M1-R0` 与当前 `SPEC.md / DESIGN.md / DESIGN-WEB.md / TECHNICAL.md`。
- 预计读取：`67d77d7` 直接差异及当前账户迁移、阅读和桌面设计事实；开始前重取所有 worktree 快照。
- 预计写入：隔离集成工作树中的阅读领域、服务端、契约、桌面 Web、测试和本台账；同一文件同一时刻只有一个写入者。

### 验收标准

1. 真实 EPUB、TXT 和已覆盖的文本 PDF 从文件入口导入隔离 PostgreSQL，图片型 PDF 保留可理解失败记录且不污染书架。
2. 所有新增表和读写显式带当前账户归属，跨账户访问失败，刷新后恢复阅读位置、划线、想法和笔记。
3. 桌面真实浏览器完成导入 → 书架 → 阅读 → 保存 / 编辑 → 刷新恢复，并覆盖加载、真实空、筛选空、失败和正常状态。
4. 定向测试、相关回归、生产构建、隔离数据库 Case、实际 diff 和当前事实源对账通过，形成范围单一的本地提交。

### 暂停条件与禁止动作

- 任一 worktree 快照相对 M1-R0 改变、目标文件出现归属不明改动，或 `67d77d7` 的适配需要改变当前产品 / 视觉规则时，回写证据并暂停。
- 禁止直接合入旧父链、复用共享 `public` 数据、弱化 E0 账户约束或修改其他 worktree 来消除冲突。

### 证据位置与恢复动作

- 证据位置：本台账 `M1-R0` 图 / 表、`67d77d7` 直接 diff、隔离 schema 测试输出与真实浏览器证据。
- 恢复动作：确认 `main` 仍为 `2b639e0`、M1-R0 台账提交已在目标基线、所有 worktree 快照无冲突，再将 `M1-F2` 改为 `ACTIVE` 并创建隔离集成工作树。

## 当前阻塞与授权门

- M1-R0 已完成；共享 PostgreSQL 和其他 worktree 均不是可写验收环境。`M1-F2` 开始前必须先建立隔离工作树与隔离 schema，并确认最新快照未出现新冲突。
- 视觉事实漂移 `8688b6f`、产品事实漂移 `925bbc1` 和 `a076 / 6f5c / 0204` 未提交内容均停在确认门外，不影响按当前事实源执行 M1-F2。
- 真实模型、微信登录、微信读书、邮件服务、Presenton 真实服务、远端 merge / push、PR、发布和部署均需在对应工作包前取得明确授权；缺少授权不通过假成功掩盖。
