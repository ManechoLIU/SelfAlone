# SelfAlone 当前任务台账

> 这是唯一执行控制面，只保存当前工作包、依赖、状态、验收、证据、阻塞和恢复动作。产品、视觉和技术规则只链接到权威文档；完成历史由 Git 保存。

## 使用规则

- 状态：`PENDING` 依赖未满足，`READY` 可开始，`ACTIVE` 正在执行，`VERIFY` 等待验收收口，`BLOCKED` 无法继续，`DONE` 已有完整证据，`SUPERSEDED` 已被替代。
- 同一时刻最多一个 `ACTIVE` 工作包。开始、完成、阻塞或拆分工作包时必须同步本文件。
- 一个开发工作包按“开发 → 定向与相关回归 → 真实业务 Case → 修复与优化 → 复验 → diff 审查 → 本地 Commit → 台账回写”闭环；远端 push、合并、PR、发布和部署另行授权。
- 界面功能使用真实浏览器或 Computer Use 拟真验收；后端、迁移和文档使用与风险相称的集成、恢复或链接证据。Agent 报告本身不算证据。

## 当前指针

- 当前版本：`redesign-v2`
- 当前分支：`main`
- 当前活动项：无
- 唯一下一项：`M1-R0 未合并候选链对账`
- 产品事实：[`redesign-v2/SPEC.md`](redesign-v2/SPEC.md)
- 技术事实：[`redesign-v2/TECHNICAL.md`](redesign-v2/TECHNICAL.md)
- 里程碑方案：[`docs/superpowers/plans/2026-08-23-mvp-launch.md`](docs/superpowers/plans/2026-08-23-mvp-launch.md)

## 里程碑

| 里程碑 | 状态 | 可观察结果 | 下一门禁 |
| --- | --- | --- | --- |
| M0 可连续点击的桌面闭环 | `DONE` | PostgreSQL 中完成开发数据的 PPT 四阶段、刷新恢复和本地 PPTX 下载 | 不把开发适配器或种子账户冒充真实用户闭环 |
| M1 真实桌面内测闭环 | `READY` | 一位邀请用户通过邮箱账户完成真实文件、阅读笔记、模型与 PPTX 闭环 | 先完成 `M1-R0` 候选对账，再进入分项集成 |
| M2 国内双端候选 | `PENDING` | 微信登录、微信读书和小程序复用同一账户及后端 | M1 完成且微信外部验证获授权 |
| M3 邀请制上线 | `PENDING` | 国内服务器与小程序候选可回滚地向受邀用户开放 | M2、适用外部验收及上线授权完成 |

## 工作包队列

| ID | 粒度 / 模式 | 状态 | 目标 | 依赖 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| GOV-01 | 功能级 / 治理 | `DONE` | 建立五层上下文职责、项目记忆、知识索引、Goal 循环与可恢复台账 | M0 | 以 `M1-E0` 试运行 |
| M1-E0 | 场景级 / 探索 | `DONE` | 对账账户归属、M0 无账户数据和安全迁移路径 | GOV-01 | 进入 `M1-R0` |
| M1-R0 | 场景级 / 探索 | `READY` | 对账未合并 M1 候选链，确定唯一集成顺序与废弃项 | M1-E0 | 先建立提交依赖图和重叠矩阵 |
| M1-F1 | 功能级 / 建设 | `PENDING` | 邮箱注册、登录、找回密码和服务端会话 | M1-R0 | 审查并复用 `658ba50` 的有效部分 |
| M1-F2 | 功能级 / 建设 | `PENDING` | 真实 EPUB/TXT/文本 PDF 导入、阅读位置、划线、想法和老己笔记 | M1-R0 | 审查并复用 `67d77d7` 及后续视觉候选 |
| M1-F3 | 功能级 / 建设 | `PENDING` | 四家固定文本模型配置、加密保存、检测与调用 | M1-R0 | 审查并复用 `3e4b020` 的有效部分 |
| M1-F4 | 功能级 / 建设 | `PENDING` | 免费体验领取、成本账本与 `¥5` 事务硬上限 | M1-F1、M1-F3 | 等待前置项 |
| M1-F5 | 功能级 / 建设 | `PENDING` | 固定版本 Presenton、三套模板、停止、失败和恢复 | M1-R0、M1-F2、M1-F3 | 对账两个 Presenton 候选后择一 |
| M1-V1 | 场景级 / 验收 | `PENDING` | 邮箱登录至真实 PPTX 下载的完整 Case 与残留修复 | M1-F1–F5 | 等待前置项及真实调用授权 |

执行中若某项触发高风险或出现长时间无证据、大范围改动、反复换方案，先拆成更小场景项；低风险机械子项只有独立验收价值时才进入台账。

### 未合并候选证据

以下内容不在 `main`，不能标记为完成，也不得直接当作当前实现；进入对应工作包前必须先审查依赖链、实际 diff、测试和真实 Case，优先复用有效部分而不是重复开发：

- `67d77d7`：M1 本地阅读候选；后续链包含桌面阅读视觉修订。
- `658ba50`：账户范围邮箱认证候选；依赖前述阅读链，当前共享 PostgreSQL 中已出现其账户、会话和邮箱令牌结构。
- `3e4b020`：加密文本模型配置候选。
- `39b84a4` 与 `d5ee177`：来自不同父提交的两个 Presenton 候选，必须择一对账，不能双重合并。
- `42d85fe` / `925bbc1`：微信小程序与连续阅读候选。
- 工作树 `0204`、`6f5c`、`a076` 仍有未提交改动；这些改动归原执行者所有，当前 Goal 不覆盖、不回退、不提交。

### GOV-01 完成证据

- 职责与恢复规则：[`AGENTS.md`](AGENTS.md)；项目记忆：[`MEMORY.md`](MEMORY.md)；知识入口：[`WIKI_INDEX.md`](WIKI_INDEX.md)。
- 当前台账已移除产品决定和视觉验收历史，只保留 M0–M3、M1 工作包、唯一下一项和 `M1-E0` 的完整恢复字段。
- 2026-08-24 验证：检查本次治理文档中的 `48` 个本地链接，失效链接为 `0`；文档差异格式检查通过；恢复检查能唯一定位 `M1-E0`，并确认目标、非目标、依赖、验收、暂停条件、证据和恢复动作齐全。
- 边界：本项没有修改运行代码、产品行为、视觉规则或远端状态。

### M1-E0 完成证据

- 领域规则：`packages/domain/src/account-identity.ts` 覆盖新身份独立账户、重新验证、退役空账户、拒绝两个有数据账户合并和重复绑定无副作用。
- 数据迁移：`apps/server/src/account-migration.ts` 在单事务中创建账户 / 身份基础、保留已有 owner、回填六张 M0 表、建立 `NOT NULL`、账户外键、父子同账户外键和账户级任务幂等索引；重复执行稳定，后段约束失败时整笔回滚。
- 运行边界：M0 种子、查询、更新、任务、页面和产物均显式限定 `account-development-local`，不再依赖无归属的全局数据。
- 自动验证：2026-08-24 运行 `pnpm verify`，类型检查、7 个测试文件的 23 个断言和生产构建通过；身份外部键唯一约束完成红灯突变与恢复复验。
- 真实 PostgreSQL：迁移连续初始化两次成功；迁移前后仍为书籍 `10`、会话 `3`、草稿 `3`、任务 `1`、页面 `3`、产物 `1`，空 owner 为 `0`；保留原 3 个 UUID 账户并只补建缺失的开发账户，11 条账户 / 同账户父子约束和 `(account_id, idempotency_key)` 唯一索引存在。
- 真实边界：本项是后端与迁移工作，没有新增用户界面，因此不做形式化 Computer Use；邮箱 / 微信认证、数据账户合并、外部调用和候选分支集成仍未完成。

## 当前可执行项：M1-R0

### 目标

对账所有未合并 M1 候选提交、依赖链、工作树 diff 和共享环境副作用，为邮箱认证、阅读、模型、小程序与 Presenton 各选出唯一主候选，形成可验证、可回退且不重复开发的 `main` 集成顺序。

### 非目标

- 不修改候选实现，不修复候选 Bug，不创建新的功能替代品。
- 不清理、回退、提交或覆盖 `0204`、`6f5c`、`a076` 的未提交改动。
- 未获授权不 cherry-pick、merge、push、建 PR、发布或部署。

### 依赖与范围

- 依赖：`M1-E0` 和本台账的“未合并候选证据”。
- 预计读取：全部 worktree 状态、候选提交父链与 diff、候选自带测试、当前 `SPEC.md / TECHNICAL.md` 和共享 PostgreSQL schema；默认不写运行代码。
- 预计写入：本台账中的主候选、被替代项、冲突、验证缺口、集成批次和授权点。

### 验收标准

1. 列出每个候选的父提交、改动文件、功能范围、测试证据、共享环境副作用和未提交状态。
2. 标出候选之间的依赖、同文件冲突、重复实现和事实源漂移；Agent 报告或提交标题不能替代实际 diff。
3. 为 M1-F1、F2、F3、F5 各选择一个主候选或明确判定重做，并说明不选其他候选的证据。
4. 给出分批集成顺序、每批真实验收、失败回退点和需要用户授权的 merge / push 节点。
5. 台账中只留下一个唯一下一工作包，未被选择的候选标记 `SUPERSEDED` 或保留为只读证据。

### 暂停条件与禁止动作

- 候选工作树仍在被其他任务写入、提交依赖不可达、无法确认未提交改动归属，或选择会改变已确认产品 / 视觉规则时暂停。
- 禁止为了让提交容易合并而丢弃真实测试、弱化安全边界或把共享数据库状态伪装成当前分支功能。

### 证据位置与恢复动作

- 证据位置：本台账“未合并候选证据”、`git log --all --graph`、`git worktree list`、各候选实际 diff 与测试输出。
- 恢复动作：核对 `main` HEAD、所有 worktree HEAD / diff 和共享数据库 schema 是否变化；无其他 `ACTIVE` 项后将 `M1-R0` 改为 `ACTIVE`，第一步只生成提交依赖图，不修改代码。

## 当前阻塞与授权门

- `M1-R0` 当前无外部阻塞，但发现候选或工作树仍在变化时必须重新快照。
- 真实模型、微信登录、微信读书、邮件服务、远端 push、合并、PR、发布和部署均需在对应工作包前取得明确授权与可用凭证；缺少授权不通过假成功掩盖。
