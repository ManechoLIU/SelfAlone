# SelfAlone 当前任务台账

> 这是唯一执行控制面，只保存当前工作包、依赖、状态、验收、证据、阻塞和恢复动作。产品、视觉和技术规则只链接到权威文档；完成历史由 Git 保存。

## 使用规则

- 状态：`PENDING` 依赖未满足，`READY` 可开始，`ACTIVE` 正在执行，`VERIFY` 等待验收收口，`BLOCKED` 无法继续，`DONE` 已有完整证据，`SUPERSEDED` 已被替代。
- 同一时刻最多一个 `ACTIVE` 工作包。开始、完成、阻塞或拆分工作包时必须同步本文件。
- 一个开发工作包按“开发 → 定向与相关回归 → 真实业务 Case → 修复与优化 → 复验 → diff 审查 → 本地 Commit → 台账回写”闭环；远端 push、合并、PR、发布和部署另行授权。
- 界面功能使用真实浏览器或 Computer Use 拟真验收；后端、迁移和文档使用与风险相称的集成、恢复或链接证据。Agent 报告本身不算证据。

## 当前指针

- 当前版本：`redesign-v2`
- 当前分支：`main`
- 当前活动项：无
- 唯一下一项：`M1-E0 账户归属与 M0 数据迁移对账`
- 产品事实：[`redesign-v2/SPEC.md`](redesign-v2/SPEC.md)
- 技术事实：[`redesign-v2/TECHNICAL.md`](redesign-v2/TECHNICAL.md)
- 里程碑方案：[`docs/superpowers/plans/2026-08-23-mvp-launch.md`](docs/superpowers/plans/2026-08-23-mvp-launch.md)

## 里程碑

| 里程碑 | 状态 | 可观察结果 | 下一门禁 |
| --- | --- | --- | --- |
| M0 可连续点击的桌面闭环 | `DONE` | PostgreSQL 中完成开发数据的 PPT 四阶段、刷新恢复和本地 PPTX 下载 | 不把开发适配器或种子账户冒充真实用户闭环 |
| M1 真实桌面内测闭环 | `READY` | 一位邀请用户通过邮箱账户完成真实文件、阅读笔记、模型与 PPTX 闭环 | 先完成 `M1-E0`，再进入认证和真实数据功能 |
| M2 国内双端候选 | `PENDING` | 微信登录、微信读书和小程序复用同一账户及后端 | M1 完成且微信外部验证获授权 |
| M3 邀请制上线 | `PENDING` | 国内服务器与小程序候选可回滚地向受邀用户开放 | M2、适用外部验收及上线授权完成 |

## 工作包队列

| ID | 粒度 / 模式 | 状态 | 目标 | 依赖 | 下一步 |
| --- | --- | --- | --- | --- | --- |
| GOV-01 | 功能级 / 治理 | `DONE` | 建立五层上下文职责、项目记忆、知识索引、Goal 循环与可恢复台账 | M0 | 以 `M1-E0` 试运行 |
| M1-E0 | 场景级 / 探索 | `READY` | 对账账户归属、M0 无账户数据和安全迁移路径 | GOV-01 | 读取当前实现并形成可验证迁移设计 |
| M1-F1 | 功能级 / 建设 | `PENDING` | 邮箱注册、登录、找回密码和服务端会话 | M1-E0 | 等待前置项 |
| M1-F2 | 功能级 / 建设 | `PENDING` | 真实 EPUB/TXT/文本 PDF 导入、阅读位置、划线、想法和老己笔记 | M1-F1 | 等待前置项 |
| M1-F3 | 功能级 / 建设 | `PENDING` | 四家固定文本模型配置、加密保存、检测与调用 | M1-F1 | 等待前置项 |
| M1-F4 | 功能级 / 建设 | `PENDING` | 免费体验领取、成本账本与 `¥5` 事务硬上限 | M1-F1、M1-F3 | 等待前置项 |
| M1-F5 | 功能级 / 建设 | `PENDING` | 固定版本 Presenton、三套模板、停止、失败和恢复 | M1-F2、M1-F3 | 等待前置项 |
| M1-V1 | 场景级 / 验收 | `PENDING` | 邮箱登录至真实 PPTX 下载的完整 Case 与残留修复 | M1-F1–F5 | 等待前置项及真实调用授权 |

执行中若某项触发高风险或出现长时间无证据、大范围改动、反复换方案，先拆成更小场景项；低风险机械子项只有独立验收价值时才进入台账。

### GOV-01 完成证据

- 职责与恢复规则：[`AGENTS.md`](AGENTS.md)；项目记忆：[`MEMORY.md`](MEMORY.md)；知识入口：[`WIKI_INDEX.md`](WIKI_INDEX.md)。
- 当前台账已移除产品决定和视觉验收历史，只保留 M0–M3、M1 工作包、唯一下一项和 `M1-E0` 的完整恢复字段。
- 2026-08-24 验证：检查本次治理文档中的 `48` 个本地链接，失效链接为 `0`；文档差异格式检查通过；恢复检查能唯一定位 `M1-E0`，并确认目标、非目标、依赖、验收、暂停条件、证据和恢复动作齐全。
- 边界：本项没有修改运行代码、产品行为、视觉规则或远端状态。

## 当前可执行项：M1-E0

### 目标

在不接入真实认证、不改变已确认账户规则的前提下，明确 `accounts / login_identities` 与 M0 六张业务表的归属关系，给出现有开发数据可保留、可验证、无静默合并的数据迁移闭环，为 M1-F1 提供稳定基础。

### 非目标

- 不实现邮箱或微信登录界面、邮件发送、密码找回和外部身份调用。
- 不实现两个已有数据账户的合并，也不扩展组织、角色或多租户权限。
- 不顺带开发文件导入、模型、免费体验或 Presenton。
- 不执行不可恢复的数据删除，不接触用户未授权的远端与生产环境。

### 依赖与范围

- 直接事实：[`SPEC.md`](redesign-v2/SPEC.md) 的账户与身份绑定规则、[`TECHNICAL.md`](redesign-v2/TECHNICAL.md) 的身份与数据约束。
- 预计读取：`apps/server/src/m0-runtime.ts`、相关领域模型、测试和数据库启动配置。
- 预计写入：账户 / 身份领域规则、数据库迁移或 M0 兼容层、定向测试；实际文件以探索证据为准并在改动前回写。

### 验收标准

1. 每张用户数据表都有明确、受约束且可审计的 `account_id` 归属，M0 现有开发数据被映射到确定的开发账户且不丢失。
2. 邮箱、Web 微信和小程序微信身份使用稳定的外部唯一键；未绑定账户保持独立。
3. 覆盖“新身份创建独立账户”“重新验证后绑定空侧”“两个已有数据账户均含数据时拒绝”“重复请求不重复绑定”四类规则。
4. 迁移可在真实 PostgreSQL 从当前 M0 状态前向执行；失败不会留下半迁移状态，重复执行结果稳定。
5. 定向领域测试、数据库集成测试、类型检查与相关回归通过；实际 diff 无无关重构。
6. 证据、剩余风险、下一项和本地提交已回写本台账。

### 暂停条件与禁止动作

- 发现当前数据库可能含非开发用户数据、账户归属无法无损确定、需要改变产品账户规则或需要破坏性迁移时，标记 `BLOCKED` 并请求确认。
- 禁止用邮箱字符串、会话 ID 或客户端来源临时冒充稳定账户；禁止静默合并、复制或删除已有账户数据。
- 真实微信、邮件、模型调用以及 push、合并、PR、发布、部署不属于本项授权。

### 证据位置与恢复动作

- M0 基线证据：提交 `f85299b424df885698fd2c23ff6f4d109862c641` 及现有 PostgreSQL / 浏览器 / PPTX 验收记录。
- M1-E0 证据：执行后回写迁移文件、测试命令与结果、数据前后核对、本地提交。
- 恢复动作：读取本项、核对仓库根与当前 HEAD / diff，确认无其他 `ACTIVE` 项后将本项改为 `ACTIVE`；第一步只读梳理当前六张表和种子数据的归属缺口。

## 当前阻塞与授权门

- `M1-E0` 当前无外部阻塞。
- 真实模型、微信登录、微信读书、邮件服务、远端 push、合并、PR、发布和部署均需在对应工作包前取得明确授权与可用凭证；缺少授权不通过假成功掩盖。
