# SelfAlone 当前任务台账

> 唯一执行控制面。这里只记录工作包、依赖、状态、验收、证据、阻塞、回退和下一步；产品、视觉与技术规则只链接到权威文档，不在台账内另造规则。

## 1. 使用规则

- 状态只使用 `PENDING / READY / ACTIVE / VERIFY / BLOCKED / DONE / SUPERSEDED`；同一时刻最多一个 `ACTIVE`，并始终保留唯一下一工作包。
- 权威顺序：[`SPEC.md`](redesign-v2/SPEC.md) 定义产品行为；[`DESIGN.md`](redesign-v2/DESIGN.md) 定义共享视觉；[`DESIGN-WEB.md`](redesign-v2/DESIGN-WEB.md) 与 [`DESIGN-MINIAPP.md`](redesign-v2/DESIGN-MINIAPP.md) 定义端侧规则；[`design-reference/README.md`](redesign-v2/design-reference/README.md) 定义认可参考的层级、适用范围与排除项；[`TECHNICAL.md`](redesign-v2/TECHNICAL.md) 定义技术边界。
- 不设置“先搭功能框架、以后补 UI”的工作包。任何用户可见纵向切片只有同时完成真实功能、账户级持久化、完整状态、认可视觉还原、端侧适配、真实入口验收和独立审查，才可标记 `DONE`。
- 参考图不是运行时整图或示例数据来源；只能采用索引明确列出的布局、节奏、状态和组件关系，运行时使用真实数据、正式头像、透明桌宠资产、成熟图标与可访问文字。
- 开发循环：实现 → 定向与相关回归 → 隔离 PostgreSQL / 对象存储 Case → 真实浏览器或微信开发者工具 Case → 视觉对照 → 修复复验 → diff 审查 → 范围单一的本地 Commit → 台账回写。
- `merge / push / PR / 发布 / 部署 / 付费或外部真实调用` 均停在独立授权门；共享数据库、提交标题、Agent 报告、旧截图或文件存在不能冒充当前主线功能。

## 2. 当前指针与真实边界

- 当前版本：`redesign-v2`
- 当前台账快照：detached 工作树 `3175` @ `2a4cc24`，其父链包含 `main` @ `2b639e0`；本次只修改本文件。
- 唯一主线：`main` @ `2b639e0`
- 当前活动项：无
- 唯一下一工作包：`M1-F2 桌面 Web 真实书架、阅读与笔记闭环`
- 当前状态：`BLOCKED`，只停在解锁门；不得开始候选重放或功能修改。
- 解锁条件：重新冻结全部 worktree；确认 `0204` 中仍在变化的阅读 / 视觉未提交现场不会成为新的认可候选；按 `SPEC.md` 明确图片型 PDF 的“可浏览页面并创建独立笔记”实现路径；建立隔离集成工作树与独立 PostgreSQL schema。
- 当前实现真相：主线只具有 M0 开发数据 PPT 闭环和 M1-E0 账户归属迁移，不具备完整真实账户、阅读、AI、额度、PPT 服务、微信或小程序 MVP；现有桌面页面是开发骨架，不是认可视觉完成态。
- 2026-08-24 真实浏览器审查：`1440×1024` 页面仍是窄侧栏加全宽 PPT 完成瀑布，缺少认可桌面外壳中的品牌远山导航、会话列表、对话上下文与右侧任务工作区；小程序候选不在主线，不能称为当前实现。

| 里程碑 | 状态 | 可观察边界 |
| --- | --- | --- |
| M0 开发底座 | `DONE` | 只证明开发数据的 PPT 四阶段、刷新恢复、本地 PPTX 与账户归属迁移；不代表真实产品或视觉完成。 |
| M1 桌面 Web 内测闭环 | `BLOCKED` | 必须按本台账完成账户、阅读、AI、额度、PPT 以及所有适用桌面视觉；当前停在 F2 解锁门。 |
| M2 国内双端候选 | `PENDING` | 必须完成微信、微信读书和小程序全部认可视觉及双端共享数据 Case；现有候选不计完成。 |
| M3 邀请制上线 | `PENDING` | M2、发布就绪、H5、回滚演练和独立发布授权全部通过后才可进入。 |

## 3. 统一完成门槛

每个 Web 或小程序工作包都必须同时满足以下门槛；工作包只补充专属 Case，不得降低这些标准。

1. **产品闭环**：对应 [`SPEC.md`](redesign-v2/SPEC.md) 行为、对象、状态、恢复和“不做”边界全部可追踪；没有只做入口、静态页面或空壳 API。
2. **真实数据**：所有用户数据带 `account_id`，刷新、跨页面和同账户跨端恢复成立；跨账户访问失败，旧请求不得覆盖新状态。
3. **完整状态**：数据区覆盖加载、真实空、搜索 / 筛选空、失败和正常；异步操作覆盖默认、禁用、运行、停止、失败、重试、成功与过期响应。
4. **视觉还原**：先按共享与端侧规范实施，再逐张检查本工作包绑定的认可参考；不得继承参考图排除项、探索稿、伪图标或示例内容。
5. **端侧验收**：桌面 Web 检查 `768 / 1024 / 1200 / 1440px`；微信小程序检查 `360 / 390 / 430px`、顶部系统区、底部手势区和键盘打开态。
6. **可用性**：无横向溢出、裁切、遮挡和焦点丢失；键盘顺序、可访问名称、非颜色单一状态、`44×44px` 目标、对比度、200% 文本和减少动态效果通过。
7. **工程证据**：相称的 H0–H4、隔离数据库 / 对象存储、真实文件或外部适配 Case、生产构建、实际 diff 和非作者定向审查通过；形成可回退的单一提交。

## 4. 产品与视觉覆盖矩阵

| 产品能力 | 唯一交付工作包 | 桌面 Web 视觉基线 | 微信小程序视觉基线 | 不可缺少的验收 |
| --- | --- | --- | --- | --- |
| 账户、邮箱、微信身份、绑定、修改邮箱 / 密码、退出 | `M1-F1`、`M2-F1` | 01 登录注册、09 设置总览 | 21 登录、22 设置总览 | 两种登录主次、验证 / 找回 / 绑定冲突、两个有数据账户拒绝合并、同账户跨端、独立账户不串数据 |
| 会话、任务互斥、草稿恢复、文字与最多 4 图消息、讨论整理笔记 | `M1-F3B`、`M2-F3` | 02 对话共享外壳、10 笔记结果、34 桌面桌宠 | 11 抽屉、13 会话、19 笔记结果、30–32 选择与输入、12 / 35 桌宠 | 新建 / 切换 / 删除、单会话锁、停止恢复、图片失败保留、其他空闲会话可发送；整理默认新增笔记，只有明确引用已有笔记才更新 |
| 统一书架、本地导入、阅读、划线、想法、老己笔记、作品 | `M1-F2`、`M2-F2`、`M2-F4` | 03 书架、07 详情结构、08 作品、10 笔记 / 划线 | 14 书架、15–17 阅读、18–20 书籍内容 | EPUB/TXT/文本 PDF、图片页面浏览、本地书统一动态默认封面、目录、进度、背景、专注、引用到对话、笔记 CRUD 与失败恢复 |
| 微信读书连接、自动同步与个人划线 / 想法 | `M2-F2` | 07 详情结构、09 设置 | 14 书架、18 划线、22 设置 | 官方 `wrk-...`、首 / 增量同步、无效 Key、换号隔离、无完整正文承诺、失败保留旧快照 |
| 文本 / 图片模型配置与真实 AI 对话 | `M1-F3A`、`M1-F3B`、`M2-F5` | 09 设置、37 文本模型配置、02 会话 | 22 设置、39 文本模型配置、13 会话 | 四家固定目录、千问业务空间、加密、成功后替换、撤销、图像能力前置检查、真实调用授权 |
| 免费体验领取、平台能力、`¥5` 硬上限与恢复 | `M1-F4`、`M2-F5` | 36 会话额度条，并借鉴 38 的组件色彩关系 | 38 会话额度条 | 一次领取、领取后消失、不显示余额、并发成本上限、耗尽后原位配置引导并返回原上下文 |
| 单书 PPT 双入口、范围、大纲、模板、生成、失败、作品与下载 | `M1-F5`、`M2-F6` | 02 范围共享外壳、04 大纲、05 模板结构、06 生成中、33 失败、08 作品；完成态按 02 / 06 派生 | 23–29 全流程、20 作品 | 来源继承 / 选择、动态追问、失效选择、连续大纲、任务锁、停止 / 重启 / 失败 / 删除、历史作品、可编辑 PPTX |
| 全产品视觉、状态、跨端同步与上线候选 | `M1-V1`、`M2-V1`、`M3-R1` | 所有适用桌面参考 | 所有适用小程序参考 | 同一真实账户从登录、读书、记录、对话到 PPTX；两端业务一致但布局不机械缩放 |

编号与文件的唯一解释见 [`design-reference/README.md`](redesign-v2/design-reference/README.md)。桌面模板选择 05 仅为流程 / 结构参考，桌面生成完成仍无认可页面参考，必须从 02 的共享外壳、06 的瀑布和当前规范派生最小变体；小程序 PPT 已由 23–29 完整覆盖。

## 5. 里程碑与工作包队列

| ID | 状态 | 目标 / 可观察结果 | 依赖 | 完成后唯一流向 |
| --- | --- | --- | --- | --- |
| `GOV-01` | `DONE` | 建立唯一台账、事实源、Goal 与恢复规则 | M0 | `M1-E0` |
| `M1-E0` | `DONE` | 冻结账户归属和安全迁移基线 | GOV-01 | `M1-R0` |
| `M1-R0` | `DONE` | 对账所有未合并候选、父链、diff、测试、worktree 与共享 PostgreSQL | M1-E0 | `M1-R1` |
| `M1-R1` | `DONE` | 将全部产品功能、Web / 小程序视觉基线和完整验收重建为本台账 | M1-R0、用户确认 | `M1-F2` 解锁门 |
| `M1-F2` | `BLOCKED` | 桌面 Web 真实书架、导入、阅读、划线、想法、老己笔记和作品闭环；UI 与功能同批完成 | M1-R1、解锁条件 | `M1-F1` |
| `M1-F1` | `PENDING` | 桌面邮箱注册 / 登录 / 登出 / 验证 / 找回 / 修改邮箱与密码、会话 Cookie、账户设置视觉闭环 | M1-F2 | `M1-F3A` |
| `M1-F3A` | `PENDING` | 桌面文本 / 图片模型配置、四家目录、密文、检测后替换、撤销和设置视觉闭环 | M1-F1 | `M1-F3B` |
| `M1-F3B` | `PENDING` | 桌面对话、会话管理、任务互斥、文字 / 图片消息、AI 对话、联网资料边界与草稿恢复 | M1-F3A | `M1-F4` |
| `M1-F4` | `PENDING` | 桌面一次性免费体验 UI、领取、成本账本、`¥5` 事务上限、耗尽与配置返回闭环 | M1-F3B | `M1-F5` |
| `M1-F5` | `PENDING` | 桌面双入口 PPT 全流程、固定 Presenton、三模板、停止 / 失败 / 恢复、作品与真实 PPTX | M1-F4 | `M1-V1` |
| `M1-V1` | `PENDING` | 一位邀请用户在桌面 Web 从邮箱进入、导入 / 阅读 / 对话到真实 PPTX 的完整 H3 Case | M1-F1–F5 | `M2-F1` |
| `M2-F1` | `PENDING` | Web 微信扫码、小程序微信主登录 / 邮箱备选、身份绑定与小程序设置账户闭环 | M1-V1、微信授权 | `M2-F2` |
| `M2-F2` | `PENDING` | 微信读书连接、自动同步、统一书架和个人划线 / 想法跨端闭环 | M2-F1、微信读书授权 | `M2-F3` |
| `M2-F3` | `PENDING` | 小程序会话抽屉、会话主画面、结构化选择、文字 / 拍照 / 相册消息与任务互斥闭环 | M2-F1、M1-F3B | `M2-F4` |
| `M2-F4` | `PENDING` | 小程序本地书介绍、纵向分页阅读、操作层、书籍内容面板、笔记与作品闭环 | M2-F2、M1-F2 | `M2-F5` |
| `M2-F5` | `PENDING` | 小程序免费体验条、模型配置、耗尽引导与返回原上下文闭环 | M2-F3、M1-F3A、M1-F4 | `M2-F6` |
| `M2-F6` | `PENDING` | 小程序范围、大纲、编辑、模板、生成中 / 失败 / 完成和 `wx.downloadFile` / `wx.openDocument` 闭环 | M2-F4、M2-F5、M1-F5 | `M2-V1` |
| `M2-V1` | `PENDING` | 同一账户在 Web 与小程序共享书籍、笔记、会话、额度、任务与作品的双端 H3 / 适用 H4 Case | M2-F1–F6 | `M3-R1` |
| `M3-R1` | `PENDING` | 隐私、数据导出 / 删除、备份恢复、脱敏、费用 / 队列告警、镜像与兼容迁移回滚就绪 | M2-V1 | 上线授权门 |
| `M3-V1` | `PENDING` | Staging H5、受邀桌面发布、小程序审核候选与回滚演练 | M3-R1、明确发布授权 | 邀请制开放 |

## 6. 各建设工作包专属验收

### M1-F2 桌面书架、阅读与笔记

- **候选起点**：只承接 `67d77d7` 的有效直接差异；`8688b6f` 仅作代码 / 视觉反例，不承接其旧台账、视觉 token 改写或未确认资产。
- **产品范围**：统一书架、搜索、导入 EPUB/TXT/文本 PDF、图片页面浏览与独立笔记、本地书统一动态默认封面、详情、目录、全书进度、浅 / 暗背景、专注、复制 / 划线 / 想法 / 引用到对话、笔记 CRUD、作品列表；AI 整理讨论在 F3B 接入本工作包的数据边界。
- **视觉范围**：03、07、08、10；阅读器未被参考图完整覆盖的部分按 `SPEC → DESIGN → DESIGN-WEB` 派生，不从 v1 或探索稿补图。
- **真实 Case**：四类文件、无法读取文件、刷新恢复、跨账户拒绝、保存失败保留输入、五类数据状态、四个桌面宽度；隔离 schema 中核对原文件、解析结果和内容记录。
- **暂停 / 回退**：`0204` 继续变化、图片型 PDF 行为未对齐、候选适配需要改变产品 / 视觉规则或目标文件归属不明即保持 `BLOCKED`；失败只丢弃隔离分支，回退到 `2b639e0`。

### M1-F1 桌面账户与设置

- **候选起点**：只承接 `658ba50` 的认证直接差异，并重新落到 M1-E0 账户归属；不得带入其 F2 父链或旧台账。
- **产品与视觉**：完成邮箱注册、验证、登录、登出、找回 / 重置、修改邮箱 / 密码、Cookie 撤销和账户关系显示；对照 01 与 09，并覆盖扫码弹窗的未接通状态，不把微信真调用伪装为已完成。
- **真实 Case**：本地 outbox / adapter、令牌单次与过期、Cookie 刷新 / 失效、两个账户隔离、错误原位保留、`768–1440px` 与键盘路径。

### M1-F3A / M1-F3B 模型、会话与 AI

- **候选起点**：`3e4b020` 只作为文本模型配置种子；不得带入其小程序父链。图片模型配置、实际 AI 对话、消息附件、联网来源与任务互斥均按当前事实补齐。
- **产品与视觉**：F3A 对照 09、37；F3B 对照 02、10、34。配置只允许四家固定供应商，千问显示业务空间 ID；检测成功才替换。会话支持新建 / 切换 / 删除、文本 / 图片、草稿、停止与原位状态更新；用户要求整理讨论时直接新增笔记，只有明确引用已有笔记并要求修改时才更新。
- **真实 Case**：AES-GCM、并发替换 / 撤销、供应商能力目录、模型不支持图片、上传 / 调用失败保留、过期回答不回写；真实供应商与联网仅在 H4 授权后执行。

### M1-F4 免费体验

- **产品与视觉**：对照 36 并借鉴 38 的额度条组件；只显示“免费体验额度 / 领取”，领取后整条消失，不显示金额、余额、进度、倒计时或券中心。
- **真实 Case**：领取幂等、并发预占与实际结算、账户累计 `¥5` 硬上限、全站预算失败、原输入 / 会话 / PPT 草稿保留、配置成功返回原位置且不自动重放。
- **费用门**：未获平台模型、次数和预算授权，只验证确定性适配器与事务边界，不进行付费调用。

### M1-F5 桌面 PPT

- **候选起点**：`39b84a4` 是唯一 Presenton 适配器种子；与其 patch 相同的 `d5ee177` 为 `SUPERSEDED`。
- **产品与视觉**：对照 02、04、05、06、33、08；完成双入口、来源继承 / 选择、动态追问、范围摘要、连续分层大纲、三模板、可调 / 可收工作区、生成瀑布、停止、失败、删除、恢复、历史作品与下载。
- **真实 Case**：固定镜像与摘要、worker/API、幂等、重启租约、对象存储、账户归属、三份中文长标题 / 有图 / 无图 PPTX；真实 PowerPoint 与 WPS 打开、编辑、重存并检查比例、字体和溢出。
- **回退 / 授权**：失败回退至 F4 提交；Presenton 真实服务、模板资产最终锁定与外部调用在对应授权门后执行。

### M2-F1–F6 小程序完整闭环

- **F1 登录 / 账户**：对照 21、22；微信为唯一高强调入口，邮箱低一层级；验证绑定、跳过绑定、两有数据账户拒绝合并、修改邮箱 / 密码和不透明令牌。
- **F2 微信读书**：对照 14、18、22；官方 `wrk-...` 首同步 / 增量 / 无效 Key / 升级要求 / 换号，旧快照保留，不展示完整正文或手动同步常驻入口。
- **F3 会话**：对照 11、13、19、30–32 和 12 / 35；抽屉、半屏多选、摘要回写、拍照 / 相册、附件失败保留、讨论整理笔记、键盘与安全区、趴卧桌宠不遮挡操作。
- **F4 阅读**：对照 14–20；介绍首屏上滑翻走、纵向分页吸附、轻点呼出操作层、五种阅读背景、目录 / 内容 / 设置 / PPT、笔记与作品面板及五类数据状态。
- **F5 额度 / 模型**：对照 38、39；额度条、耗尽引导、固定供应商配置、键盘安全区和返回原上下文。
- **F6 PPT**：逐页对照 23–29；范围、大纲、编辑面板、双列模板、生成中 / 失败 / 完成连续瀑布、停止、删除、恢复和小程序下载 / 打开链。
- **共同真实 Case**：`360 / 390 / 430px`、系统字体、顶部系统区、底部手势区、键盘、弱网 / 中断 / 切后台恢复；微信开发者工具 H3 与真机关键路径。不得把桌面布局机械缩窄。

## 7. M1-R0 候选链结论与事实漂移

提交依赖图（箭头右侧依赖左侧；当前 `main` 不是候选链后代）：

```text
f85299b → 67d77d7 → 519801f → edcd8cf → 8688b6f
                                              ├→ 42d85fe → 925bbc1
                                              └→ 658ba50 → 96498c9 → 3a4ed35 → 3e4b020
                                                                                  ├→ c4b42eb → 39b84a4
                                                                                  └→ 2f79bda → d5ee177
f85299b → … → 2b639e0 (main，含 M1-E0)
```

| 工作包 | 唯一候选结论 | 直接边界 |
| --- | --- | --- |
| M1-F2 | `67d77d7` | 复用真实导入 / 阅读 / 标注 / 笔记直接差异；必须接入 E0 账户归属并按当前视觉重做 UI。`8688b6f` 不直接集成。 |
| M1-F1 | `658ba50` | 只复用邮箱认证直接差异；重新落位到 F2 后的运行时和 E0 账户 schema，不带祖先。 |
| M1-F3A | `3e4b020` | 只复用模型配置直接差异；真实调用、图片模型、会话和联网不在候选完成范围。 |
| M1-F5 | `39b84a4` | 与 `d5ee177` 三文件 blob 和稳定 patch-id 相同，后者 `SUPERSEDED`；候选只提供 fail-closed 适配种子。 |
| M2 | `42d85fe → 925bbc1` 只读 | 不进入 M1；`925bbc1` 把认可的纵向分页改为连续滚动，属于产品漂移，不能吸收。 |

主要重叠接缝：E0 与 F2 重叠 domain index；E0 与 F1 重叠 server app / runtime / domain index；F2 与 F1 重叠 8 条 server / Web / contract 路径；F2 与 F3 重叠 6 条入口路径；F1 与 F3 重叠 7 条入口路径。集成必须按队列顺序重排共享入口，不能整链 cherry-pick。

事实漂移与验证缺口：

- `67d77d7` 硬编码开发账户；图片型 PDF 直接失败，与 `SPEC.md` 的“页面可浏览并可创建独立笔记”冲突。产品行为以 `SPEC.md` 为准；F2 开始前必须确定不引入 OCR 的页面浏览技术路径并同步受影响技术事实。
- `8688b6f` 改写共享视觉 token、登记当前事实源没有的运行时资产并携带旧台账；只允许逐组件重新评估，不恢复其事实源。
- `658ba50` 不含微信、小程序令牌、修改邮箱 / 密码和完整绑定关系；这些缺口已分别进入 F1 与 M2-F1。
- `3e4b020` 不含真实 AI 调用、图片模型、成本账本、联网来源和当前千问目录完整验证；这些缺口已进入 F3A / F3B / F4。
- `39b84a4` 不含固定镜像、worker/API 接缝、真实停止、重启恢复、安全归档和 PowerPoint / WPS 验收；这些缺口已进入 F5。
- 小程序候选既未集成，也不符合当前纵向分页与认可参考；M2 必须按 21–32、38–39 和当前规范重新收敛，不能把候选截图或提交标题当成视觉完成。

## 8. Worktree、共享环境与阻塞证据

2026-08-24 当前快照：

| 工作树 | HEAD / 未提交状态 | 处理结论 |
| --- | --- | --- |
| 原主工作区 | `main` @ `2b639e0`；`341` 条状态项，主要为大量 `v1/` 删除和 3 个 Presenton smoke 文件 | 他人现场，只读，永不进入本任务提交。 |
| `0204` | `9783628`；`36` 条，含其自己的 `PROJECT_STATUS.md`、移动会话探索和阅读 / 设置探索图 | 条目数量相对 M1-R0 记录发生变化，归属不明且与 F2 / M2 视觉范围重叠；这是当前 F2 解锁阻塞。 |
| `146b` | `39b84a4`；干净 | F5 规范候选验证树，只读。 |
| `3175` | `2a4cc24`；改动前干净 | 本台账唯一写入位置。 |
| `60b5 / f509` | `f85299b`；干净 | 旧共同基线，只读。 |
| `6757` | `519801f`；干净 | F2 父链证据，只读。 |
| `6f5c` | `925bbc1`；`19` 条未提交状态 | M2 他人现场，不吸收、不清理。 |
| `a076` | `d5ee177`；`5` 条未提交状态 | 他人现场；提交已被 `39b84a4` 替代。 |

M1-R0 记录的共享 PostgreSQL 17.11 `public` 已有 F2 / F1 / F3 结构痕迹和来源不明的 `selfalone_visual_empty_qa` schema，且没有迁移登记表；它只是跨工作树副作用证据，不证明 `main` 功能。所有后续工作包必须使用独立 schema 或独立实例，并记录迁移前后表、数据量、owner、约束和回退点。

候选自动验证只证明旧候选快照：干净 `146b` @ `39b84a4` 的 `pnpm verify` 曾通过 19 个测试文件 / 114 项测试与生产构建；F2、F1、F3、F5 定向分别为 5、30、24、17 项。它不证明与 `2b639e0` 集成，不替代真实文件、浏览器、小程序、外部模型或 PowerPoint / WPS 验收。

## 9. 分批集成、回退与授权

| 批次 | 集成结果与真实验收 | 失败回退点 | 授权门 |
| --- | --- | --- | --- |
| 0 | 重新冻结 worktree，确认 `0204` 归属与 F2 无新认可候选；从 `2b639e0` 创建隔离工作树和独立 schema | 不创建集成分支或直接丢弃空隔离目录 | 本地隔离分支 / 提交在工作包内；合入 `main` 另行授权 |
| 1 | M1-F2 桌面阅读与视觉闭环 | `2b639e0` | 不触碰其他 worktree；候选漂移停在确认门 |
| 2 | M1-F1 桌面账户与设置 | F2 本地提交 | 真实邮件另行授权 |
| 3 | M1-F3A / F3B 模型、会话与 AI | F1 或 F3A 本地提交 | 真实模型、联网、预算另行授权 |
| 4 | M1-F4 免费体验与成本上限 | F3B 本地提交 | 付费验证另行授权 |
| 5 | M1-F5 桌面 PPT | F4 本地提交 | Presenton 真实服务、外部调用与最终模板锁定授权 |
| 6 | M1-V1 桌面完整 Case | F5 本地提交 | 通过后才请求 merge / push |
| 7 | M2-F1–F6 小程序与微信闭环 | 每个前序 M2 本地提交 | 微信登录、微信读书、真机 / 审核能力分别授权 |
| 8 | M2-V1 双端候选 | M2-F6 本地提交 | 通过后才请求主线合入 / 远端候选 |
| 9 | M3 发布就绪与邀请制上线 | 前一兼容镜像 / 迁移 | push、PR、部署、小程序提交审核和发布均需明确授权 |

## 10. 唯一恢复动作

下一次只能恢复 `M1-F2` 解锁门：

1. 核对 `main`、本台账提交和全部 worktree HEAD / 状态哈希。
2. 取得 `0204` 当前所有者对未提交阅读 / 视觉现场的归属结论；若仍在产出，保持 `BLOCKED`，不得并行实现 F2 UI。
3. 以 `SPEC.md` 为产品结论，确定图片型 PDF 的页面浏览且不做 OCR 的技术路径，并只同步真实受影响的 `TECHNICAL.md`。
4. 无新冲突后，从 `main` @ `2b639e0` 创建隔离集成工作树与独立 PostgreSQL schema，将 `M1-F2` 改为 `ACTIVE`。

除此之外没有第二个可执行工作包。禁止 cherry-pick 整条候选链、修改其他 worktree、使用共享 `public` 验收、开始小程序并行重做，或把当前开发骨架描述为已按视觉规范完成。
