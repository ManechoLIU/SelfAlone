# SelfAlone 任务台账

> 本文件是唯一执行控制面，只记录当前 Goal、任务拆分、状态、阻塞、下一步动作、验收标准和证据位置。产品规则见 [`redesign-v2/SPEC.md`](redesign-v2/SPEC.md)；共享视觉与端侧规则见 [`redesign-v2/DESIGN.md`](redesign-v2/DESIGN.md)、[`redesign-v2/DESIGN-WEB.md`](redesign-v2/DESIGN-WEB.md)、[`redesign-v2/DESIGN-MINIAPP.md`](redesign-v2/DESIGN-MINIAPP.md)；认可参考图只按 [`redesign-v2/design-reference/README.md`](redesign-v2/design-reference/README.md) 使用；技术事实见 [`redesign-v2/TECHNICAL.md`](redesign-v2/TECHNICAL.md)。

## 1. 当前 Goal 与执行波次

- 当前 Goal：解除 `M1-F2` 开工阻塞，形成可验收的桌面书架、阅读与笔记闭环起点。
- 主线：本地 `main` @ `2b639e0`；本工作树当前 `HEAD` 为本台账提交（父提交 `204b374`），尚未获授权合入 `main`。
- 当前执行波次：`M1-F2-UNLOCK`。
- 当前活动项：无
- 协调下一动作：先完成 `M1-F2-B`；并行等待 `M1-F2-A` 的现场归属结论。两项均通过后才把 `M1-F2` 改为 `READY`。

状态只使用 `PENDING / READY / ACTIVE / VERIFY / BLOCKED / DONE / SUPERSEDED`。同一波次可以有多个互不依赖、负责人和文件所有权互斥、共享环境已隔离或登记的 `ACTIVE` 工作包；否则顺序执行。每项都有自己的下一步动作，不再设置全项目“唯一下一项”。

## 2. 工作包队列

| ID | 阶段 / 粒度 | 状态 | 负责人 | 目标 | 依赖 | 下一步动作 |
| --- | --- | --- | --- | --- | --- | --- |
| `GOV-01` | 治理 / 里程碑 | `DONE` | 主 Agent | 建立唯一台账、事实源与恢复规则 | M0 | 已进入 `M1-E0` |
| `M1-E0` | 技术 / 高风险场景 | `DONE` | 主 Agent | 冻结账户归属和安全迁移基线 | GOV-01 | 已进入 `M1-R0` |
| `M1-R0` | 治理 / 验收级 | `DONE` | 主 Agent | 对账 F1/F2/F3/F5 未合并候选与共享环境 | M1-E0 | 已进入台账重建 |
| `M1-R1` | 产品与视觉 / 里程碑 | `DONE` | 主 Agent | 把 Web、小程序功能和认可视觉基线恢复到权威事实源与执行队列 | M1-R0、用户确认 | 进入 `M1-F2-UNLOCK` |
| `M1-F2-A` | 解锁 / 风险场景 | `BLOCKED` | 0204 现场所有者 + 主 Agent | 确定与阅读 / 视觉范围重叠的未提交现场归属 | M1-R1 | 归属明确后更新 F2 候选边界 |
| `M1-F2-B` | 解锁 / 技术场景 | `READY` | 主 Agent | 对齐图片型 PDF 的无 OCR 页面浏览技术路径 | M1-R1 | 完成后等待 A；两项通过则解锁 F2 |
| `M1-F2` | 建设 / 功能级 | `BLOCKED` | 待分配 | 完成桌面真实书架、导入、阅读、划线、想法、笔记与作品，功能和视觉同批验收 | F2-A、F2-B | 完成后进入 F1 |
| `M1-F1` | 建设 / 功能级 | `PENDING` | 待分配 | 完成桌面邮箱账户、会话与账户设置视觉闭环 | M1-F2 | 完成后进入 F3A |
| `M1-F3A` | 建设 / 功能级 | `PENDING` | 待分配 | 完成桌面文本 / 图片模型配置、检测、密文与撤销闭环 | M1-F1 | 完成后进入 F3B |
| `M1-F3B` | 建设 / 功能级 | `PENDING` | 待分配 | 完成桌面对话、会话管理、任务互斥、图片消息、AI 与草稿恢复 | M1-F3A | 完成后进入 F4 |
| `M1-F4` | 建设 / 高风险功能 | `PENDING` | 待分配 | 完成免费体验领取、成本账本、`¥5` 硬上限与恢复 | M1-F3B | 完成后进入 F5 |
| `M1-F5` | 建设 / 功能级 | `PENDING` | 待分配 | 完成桌面单书 PPT 双入口、范围、大纲、模板、生成、作品与可编辑 PPTX | M1-F4 | 完成后进入 M1-V1 |
| `M1-V1` | 验收 / 真实 Case | `PENDING` | 非作者验收 + 主 Agent | 桌面同一真实账户从登录、读书、记录、对话到 PPTX 的完整闭环 | M1-F1–F5 | 通过后进入 M2 波次 |
| `M2-F1` | 建设 / 功能级 | `PENDING` | 待分配 | 微信扫码、小程序微信主登录 / 邮箱备选、绑定与账户设置 | M1-V1、微信授权 | 释放 M2-F2 / F3 |
| `M2-F2` | 建设 / 功能级 | `PENDING` | 待分配 | 微信读书连接、自动同步、统一书架与个人划线 / 想法 | M2-F1 | 释放 M2-F4 |
| `M2-F3` | 建设 / 功能级 | `PENDING` | 待分配 | 小程序会话抽屉、会话、结构化选择、图片消息与任务互斥 | M2-F1、M1-F3B | 释放 M2-F5 |
| `M2-F4` | 建设 / 功能级 | `PENDING` | 待分配 | 小程序书籍介绍、纵向分页阅读、操作层、书籍内容与作品 | M2-F2、M1-F2 | 释放 M2-F6 |
| `M2-F5` | 建设 / 高风险功能 | `PENDING` | 待分配 | 小程序免费体验、模型配置、耗尽引导与原上下文恢复 | M2-F3、M1-F3A、M1-F4 | 释放 M2-F6 |
| `M2-F6` | 建设 / 功能级 | `PENDING` | 待分配 | 小程序 PPT 全流程与下载 / 打开链 | M2-F4、M2-F5、M1-F5 | 完成后进入 M2-V1 |
| `M2-V1` | 验收 / 真实 Case | `PENDING` | 非作者验收 + 主 Agent | Web 与小程序同账户数据、任务、额度和作品双端闭环 | M2-F1–F6 | 通过后进入 M3 |
| `M3-R1` | 发布准备 / 高风险场景 | `PENDING` | 待分配 | 隐私、导出 / 删除、备份恢复、脱敏、告警、镜像和迁移回滚 | M2-V1 | 进入 M3-V1 |
| `M3-V1` | 发布 / H5 | `PENDING` | 主 Agent + 人类授权 | Staging、邀请制桌面发布、小程序审核候选与回滚演练 | M3-R1、发布授权 | 邀请制开放 |

未来工作包只在进入 `READY` 前展开完整卡片。任何 Web 或小程序工作包都必须同时满足对应 `SPEC` 功能、共享视觉、目标端规范、认可参考图、完整状态、真实数据、端侧视口与可访问性验收；不能用骨架、静态页面或测试数量代替。

## 3. 当前可执行项：M1-F2-B

### 目标与边界

- 目标：确定图片型 PDF 在不做 OCR 时如何形成“页面可浏览、可独立记笔记”的真实技术路径，并同步真正受影响的技术事实。
- 非目标：不实现阅读器，不修改候选代码，不生成或伪造正文，不改变 `SPEC.md`、视觉规范或参考图已经确定的用户行为。

### 依赖、负责人和预计读写范围

- 依赖：`SPEC.md` 的本地导入 / 阅读行为、当前 `TECHNICAL.md` 和候选 `67d77d7` 的 PDF 边界。
- 负责人：主 Agent。
- 预计读取：`redesign-v2/SPEC.md`、`redesign-v2/TECHNICAL.md`、F2 候选的 PDF 解析与测试差异。
- 预计写入：仅在技术事实确实变化时更新 `redesign-v2/TECHNICAL.md` 与本台账。

### 验收标准与验证方式

1. 明确页面渲染来源、缓存 / 持久化边界、失败状态和独立笔记锚点。
2. 不依赖“PDF 内已经存在原始页面图片”，也不引入 OCR 或伪造正文。
3. 文本 PDF、图片型 PDF、加密 / 损坏 / 无法渲染 PDF 的行为与 `SPEC.md` 无冲突。
4. 完成定向链接检查、相关测试方案审查和实际 diff 审查。

验证方式：事实源对账、候选 diff 审查、链接检查、`git diff --check`；本项不以代码测试冒充技术决定完成。

### 证据位置

- 产品事实：`redesign-v2/SPEC.md`。
- 技术事实：`redesign-v2/TECHNICAL.md`。
- 候选与冲突结论：提交 `2a4cc24` 及本文件第 5 节。

### 阻塞、暂停与下一步动作

- 当前阻塞：无；状态为 `READY`。
- 暂停条件：技术选择会改变既定产品或视觉规则、需要新增付费 / 外部服务、或候选继续变化。
- 下一步动作：完成后改为 `DONE`，等待 `M1-F2-A`；两项均通过后将 `M1-F2` 改为 `READY`。

## 4. 当前阻塞项：M1-F2-A

- 目标：确认 `0204` 工作树内与阅读 / 设置 / 视觉范围重叠的未提交内容是谁的、是否仍在产出、是否形成新候选。
- 非目标：不读取内容后擅自吸收，不修改、提交、清理、回退或覆盖该工作树。
- 依赖 / 负责人：需要 `0204` 当前所有者给出归属结论；主 Agent 只做只读核验。
- 范围：`0204` 的 HEAD、状态摘要、相关 diff 与产物身份。
- 验收：归属、是否冻结、与 F2 的重叠、复用 / 排除结论和证据位置全部明确。
- 验证：重新核对 worktree HEAD / status / 相关 diff；不得把文件名、截图或 Agent 报告当成完成证据。
- 证据：M1-R0 提交 `2a4cc24`；上次快照为 `0204` @ `9783628`、36 条状态项，当前事实恢复时必须重验。
- 阻塞：所有者结论缺失；恢复点为只读状态核验。
- 下一步动作：归属明确后更新 F2 候选边界；仍在产出则继续 `BLOCKED`。
- 回滚 / 禁止 / 确认：无写入所以无需回滚；禁止触碰其未提交内容；归属或产品 / 视觉规则冲突必须人类确认。

## 5. 已完成对账与可复用候选

| 工作包 | 唯一结论 | 证据与不可跨越边界 |
| --- | --- | --- |
| `M1-F2` | `67d77d7` 仅作功能种子 | 只复用真实导入 / 阅读 / 标注 / 笔记的直接差异；账户归属和 UI 按当前事实重落。`8688b6f` 不直接集成。 |
| `M1-F1` | `658ba50` 仅作邮箱认证种子 | 不带入 F2 父链；微信、小程序令牌、修改邮箱 / 密码和绑定关系仍是缺口。 |
| `M1-F3A` | `3e4b020` 仅作文本模型配置种子 | 不带入小程序父链；真实调用、图片模型、会话、联网和成本账本未完成。 |
| `M1-F5` | `39b84a4` 为唯一 Presenton 种子 | `d5ee177` patch 等价并 `SUPERSEDED`；固定镜像、worker/API、停止恢复、安全归档和 PowerPoint / WPS 验收仍缺。 |
| `M2` | `42d85fe → 925bbc1` 只读参考 | 不进入 M1；连续滚动与认可的纵向分页冲突，小程序 UI 必须按当前规范与 21–32、38–39 参考重做。 |

完整父链、文件重叠、测试与共享 PostgreSQL 对账保存在提交 `2a4cc24` 的本文件历史中。共享 `public` schema 只证明跨工作树副作用，不证明 `main` 已有功能；后续开发必须使用隔离 schema 或独立实例。

## 6. 最近证据与授权门

| 工作包 | 候选 / 环境 | 验证方式 | 结果 | 证据位置 / 失效条件 |
| --- | --- | --- | --- | --- |
| `M1-E0` | `main` @ `2b639e0` | 迁移、账户域与相关测试 | `DONE` | 提交 `2b639e0`；账户 schema 或迁移事实变化则失效 |
| `M1-R0` | 候选链与 worktree 快照 | Git 图、patch-id、diff、候选测试、共享 PostgreSQL 只读核验 | `DONE` | 提交 `2a4cc24`；候选、worktree 或共享环境变化只使相关结论失效 |
| `M1-R1` | 产品 / 视觉台账重建 | 权威文档与参考索引对账、diff 审查 | `DONE` | 提交 `6a29b96`、`204b374`；产品或视觉事实变化则更新对应项 |
| 治理校正 | 当前 `HEAD` | 文章字段对照、规则与台账 diff 审查 | `DONE` | 本次范围单一本地提交；台账或协作规则再次变化则失效 |

- 本地隔离分支 / worktree 和范围单一提交可在工作包内执行；不得触碰其他 worktree 未提交内容。
- merge 到 `main`、远端 push、PR、发布、部署、真实邮件 / 微信 / 模型 / Presenton 调用、付费验证和小程序审核分别需要明确授权。
- 每批失败回退到前一批已验证本地提交；共享数据库迁移前后记录 schema、表、数据量、owner、约束和回退点。
