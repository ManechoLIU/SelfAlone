# SelfAlone 全局规则

本文件适用于整个仓库。子目录规则只能补充局部要求，不得改变这里的事实源、版本边界和安全规则。

## 1. 事实源与版本

- `README.md`：项目入口和文档导航，不承载需求规则。
- `redesign-v2/SPEC.md`：产品目标、功能、对象、流程、状态和验收。
- `redesign-v2/DESIGN.md`：共享视觉规范和唯一视觉文档入口；文件为空时不得从旧版推断设计。
- `redesign-v2/DESIGN-WEB.md`：桌面 Web 专属布局、组件、交互、动效和视觉验收。
- `redesign-v2/DESIGN-MINIAPP.md`：微信小程序专属布局、组件、交互、动效和视觉验收。
- `redesign-v2/design-reference/README.md`：全项目统一使用的设计参考图、确认层级、适用范围及原稿链接的唯一索引；不独立定义产品或视觉规则。
- `redesign-v2/prototype/`：当前原型；不存在时不得把旧原型称为当前实现。
- `EVOLUTION.md`：经验、错误与方法论，不是需求事实源。
- `v1/`：只读历史。除非用户明确要求，不得修改或从中默认继承规则。

同一规则只保留一个当前事实源，不新增平行 PRD、设计规范或日期版主文档。三份视觉文档按“共享入口 / 桌面 Web / 微信小程序”分层，不得在端侧文件复制共享规则，也不得跨端继承另一端的专属规则。

所有会话和 agent 统一按以下顺序解析定稿，不得各自维护一套“记忆中的定稿”：产品行为以 `SPEC.md` 为准；共享视觉先读 `DESIGN.md`，桌面 Web 再读 `DESIGN-WEB.md`，微信小程序再读 `DESIGN-MINIAPP.md`；设计或开发可用图片、确认层级、适用范围及原稿链接只查 `design-reference/README.md`。会话消息、任务交接、memory、文件名和生成日期只能用于定位历史，不能覆盖仓库中的当前事实源。

“完整视觉定稿”可以约束对应页面和明确标注的共享外壳；“已确认流程 / 结构”只约束流程和内容组织。缺失的品牌与共同视觉回到 `DESIGN.md`，端侧布局与组件外观回到对应端侧规范和完整视觉定稿补齐。若任一会话记录、参考图或实现与上述文件不一致，相关 agent 必须先停止采用有争议内容、完成事实源对账，再继续设计或开发；不得按较新的会话、文件名或日期自行裁决。

进入技术设计后再创建 `redesign-v2/TECHNICAL.md`，集中承载架构、数据、接口、任务执行、存储和安全规则；技术工作开始前不创建空文档，也不从产品或视觉文档推断架构。

## 2. 工作规则

- 开始前确认工作目录、任务范围、指定资料和未提交改动；现有改动默认属于用户。
- 新参与者先读 `README.md`，再按任务只读取对应事实源；不得把无关文档全部加载为默认上下文。
- 产品、交互或原型任务先完整阅读 `redesign-v2/SPEC.md`；实际视觉设计任务再完整阅读 `redesign-v2/DESIGN.md` 和当前目标对应的 `DESIGN-WEB.md` 或 `DESIGN-MINIAPP.md`，不得加载无关端侧规范后混用。只登记已确认本地参考图时走下述快速通道。
- 实际视觉设计、原型或开发任务还必须完整阅读 `redesign-v2/design-reference/README.md`，并只在其中标明的确认层级和适用范围内使用图片。
- 先检查现有文件再修改；搜索使用 `rg`，手工编辑使用 `apply_patch`。
- 需求变更确认后，主动扫描并同步受影响的事实源、原型和测试，同时判断是否更新 `EVOLUTION.md`；不得等待用户提醒，不受影响的文件不机械修改。
- 用户在会话中确认或纠正新的产品或视觉规则后，应在同一任务内先更新实际受影响的共享或端侧事实源，并按需更新 `design-reference/README.md`；若只是把不改变现有规则的图片确认为开发参考，则只走下述快速通道。未完成必要同步前不得让其他 agent 仅凭该会话记录继续设计或开发。
- 根目录只放项目入口和全局文件；素材、截图、输出和工具进入所属版本的明确子目录。
- 只修改获批范围，不回退、覆盖、删除或整理无关内容。

## 3. 轻量文档与参考图快速通道

- 普通文案、链接或索引小改，以及将用户已明确认可、已在本地的单张参考图或同一确认批次登记进 `design-reference/`，只要不改变产品行为、视觉规则、代码或原型，均由主 Agent 直接执行；不使用完整交付编排、brainstorming、writing-plans、子代理、浏览器验收或额外计划文档。
- 普通文档小改只读取目标文件及其直接事实源。参考图登记只读取本索引的相关段落，并核对用户确认、客户端、确认层级、适用范围、原稿或来源、文件尺寸与哈希；文件身份明确时不重复评审图片，也不完整读取 `SPEC.md`、三份视觉规范或历史记录。
- 参考图本身没有引入新规则时，只复制文件并更新 `design-reference/README.md`；只有用户同时确认了新的产品或视觉规则，才同步对应的 `SPEC.md`、`DESIGN.md`、`DESIGN-WEB.md` 或 `DESIGN-MINIAPP.md`。
- 不因轻量登记机械更新根 `README.md`、`PROJECT_STATUS.md` 或 `EVOLUTION.md`。最小验证为目标 diff、相关本地链接、尺寸与哈希，以及 `git diff --check`；通过后立即停止。
- 如果来源文件不明确、确认层级或适用范围缺失、参考图与当前事实源冲突，或任务扩展到实际设计与实现，则退出快速通道，先完成对账并按对应任务规则处理。

## 4. 质量与安全

- 手机按单任务设计，不把桌面多栏机械缩窄。
- 数据界面区分加载、真实空、筛选空、失败和正常状态；失败保留输入、上下文和已完成结果，过期任务不得回写。
- 交互具备键盘焦点、可访问名称、非颜色状态提示、44×44px 触控目标，并尊重减少动态效果设置。
- 本地 Git 提交用于及时留痕、溯源和回滚：完成相称验证并审查实际 diff 后可直接提交，无需逐次授权；每个提交必须范围单一、说明清楚，不得夹带无关改动。
- 不新增生产依赖，不调用付费服务，不上传、发布或推送，除非用户明确授权。
- 凭证不得进入聊天、源码、命令参数、日志或原型数据。
- 如允许使用子代理，只能使用 `luna_worker`；`fork_turns` 使用 `"none"` 或正整数，不传模型覆盖参数。

## 5. 验证与交付

- 按风险运行目标测试、相关回归、链接检查、残留扫描和 `git diff --check`。
- 原型验证完整用户路径及相关视口，不以文件存在、静态选择器或 HTTP 成功代替真实体验验证。
- 未实际验证的结果不得声称完成；最终说明已完成内容、验证证据和真实限制。
