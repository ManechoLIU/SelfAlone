# SelfAlone 全局规则

本文件适用于整个仓库，集中保留 SelfAlone 必须自带的事实源、版本边界、安全和验收要求；子目录规则只能补充局部要求。

## 1. 事实源与版本

- `README.md`：项目入口和文档导航，不承载需求规则。
- `redesign-v2/SPEC.md`：产品目标、功能、对象、流程、状态和验收。
- `redesign-v2/DESIGN.md`：共享视觉规范和唯一视觉入口；文件为空时不得从旧版推断设计。
- `redesign-v2/DESIGN-WEB.md`：桌面 Web 专属布局、组件、交互、动效和视觉验收。
- `redesign-v2/DESIGN-MINIAPP.md`：微信小程序专属布局、组件、交互、动效和视觉验收。
- `redesign-v2/design-reference/README.md`：可用参考图、确认层级、适用范围及原稿链接的唯一索引，不独立定义产品或视觉规则。
- `redesign-v2/prototype/`：当前原型；不存在时不得把旧原型称为当前实现。
- `EVOLUTION.md`：可复用经验与方法，不是需求事实源；`v1/` 为只读历史，除非用户明确要求，不得修改或默认继承。

同一规则只保留一个当前事实源。产品行为以 `SPEC.md` 为准；共享视觉先读 `DESIGN.md`，再按目标端读取 `DESIGN-WEB.md` 或 `DESIGN-MINIAPP.md`；图片只按 `design-reference/README.md` 的确认层级和适用范围使用。会话、交接、memory、文件名和日期只能定位历史，不能覆盖当前事实源。

“完整视觉定稿”可以约束对应页面及明确标注的共享外壳；“已确认流程 / 结构”只约束流程和内容组织。缺失的共享视觉回到 `DESIGN.md`，端侧规则回到对应端侧规范。参考图或实现与事实源冲突时，先停止采用争议内容并完成对账。

进入技术设计后再创建 `redesign-v2/TECHNICAL.md`，集中记录架构、数据、接口、任务、存储和安全；技术工作开始前不创建空文档，也不从产品或视觉文档推断架构。

## 2. 工作边界

- 开始前确认仓库根目录、任务范围和未提交改动；现有改动默认属于用户，不覆盖、回退或整理无关内容。
- 只修改获批范围，优先最小可验证闭环；未经当前证据验证的行为、页面、外部调用或结果不得称为已完成。
- 完成相称验证并审查实际 diff 后可创建范围单一的本地 Git 提交；推送、PR、发布、部署、删除数据和其他难恢复操作仍需明确授权。
- 如使用子代理，只能使用 `luna_worker`，`fork_turns` 使用 `"none"` 或正整数字符串且不传模型覆盖参数；主 Agent 必须审查实际 diff 并完成最终验收。

## 3. 任务读取与修改

- 新参与者先读 `README.md`，再按任务读取对应事实源，不默认加载全部文档。
- 产品、交互或原型任务读取 `SPEC.md`；实际视觉设计再读取 `DESIGN.md`、目标端规范和 `design-reference/README.md`，不得加载另一端规范后混用。
- 用户确认新的产品或视觉规则后，同步实际受影响的事实源、原型和测试；仅确认不改变规则的本地参考图时走快速通道。
- 搜索使用 `rg`，手工编辑使用 `apply_patch`。根目录只放项目入口和全局文件；素材、截图、输出和工具进入所属版本目录。

## 4. 参考图与轻量文档快速通道

- 普通文案、链接、索引小改，以及登记用户已明确认可、已在本地的单张参考图或同一确认批次，只要不改变产品行为、视觉规则、代码或原型，均由主 Agent 直接完成；不加载编排或构思技能，不分派子代理，不创建额外计划或设计文档。
- 文档小改只读取目标文件及直接事实源。参考图只核对用户确认、客户端、确认层级、适用范围、原稿或来源、尺寸与哈希；身份明确时不重复评审图片。
- 图片没有引入新规则时，只复制文件并更新 `design-reference/README.md`。不机械更新根 `README.md`、`PROJECT_STATUS.md` 或 `EVOLUTION.md`。
- 最小验证为目标 diff、相关链接或文件元数据、尺寸与哈希以及 `git diff --check`。若来源、确认层级或范围不明，或与事实源冲突、扩展到实际设计和实现，则退出快速通道并先对账。

## 5. 项目质量与验收

- 手机按单任务、单列设计，不把桌面多栏机械缩窄。
- 数据界面区分加载、真实空、筛选空、失败和正常；失败保留输入、上下文和已完成结果，过期任务不得回写。
- 交互具备键盘焦点、可访问名称、非颜色状态提示、44×44px 触控目标，并尊重减少动态效果设置。
- 不新增生产依赖，不调用付费服务，不上传、发布或推送，除非用户明确授权；凭证不得进入聊天、源码、命令参数、日志或原型数据。
- 按风险运行定向测试、相关回归、链接检查、残留扫描和 `git diff --check`。原型须验证完整用户路径及相关视口；不以文件存在、静态选择器或 HTTP 成功代替真实体验。
- 最终说明完成内容、验证证据和真实限制；未实际验证的结果不得声称完成。
