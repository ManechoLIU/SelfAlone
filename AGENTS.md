# SelfAlone 全局规则

本文件适用于整个仓库，集中保留 SelfAlone 必须自带的事实源、版本边界、安全和验收要求；子目录规则只能补充局部要求。

## 1. 项目背景、技术栈与目录

SelfAlone（老己）是以对话为入口、覆盖个人书库、阅读笔记与 PPT 生成的国内版产品。当前开发版本是 `redesign-v2`，目标端为桌面 Web 与微信小程序；具体产品范围只以 `redesign-v2/SPEC.md` 为准。

当前已落地基线是 pnpm workspace、TypeScript strict、React + Vite 桌面端、Fastify 服务端和 PostgreSQL；`package.json` 当前要求 Node.js `>=22`，技术设计目标运行时为 Node.js 24 LTS。S3 兼容对象存储和同仓库独立 Worker 属于目标架构，完整选型、拟建模块和外部适配器边界只以 `redesign-v2/TECHNICAL.md` 为准。

当前仓库目录：

| 路径 | 当前职责 |
| --- | --- |
| `apps/web/` | 桌面 Web |
| `apps/server/` | HTTP API 与业务编排 |
| `packages/contracts/` | 共享请求、响应、状态和错误契约 |
| `packages/domain/` | 无 UI、网络和数据库依赖的领域规则 |
| `packages/presentation-adapter/` | PPT 生成适配与本地产物 |
| `infra/` | 本地及部署基础设施 |
| `data/artifacts/` | 本地开发产物，不作为完成证据 |
| `redesign-v2/` | 当前产品、视觉、技术、参考图和版本内输出 |
| `docs/superpowers/` | 已确认的阶段计划；不代替当前台账 |

`apps/miniapp/`、`apps/worker/`、`packages/test-support/` 是技术设计中的目标目录；不存在时不得描述为当前实现。

常用命令均从仓库根目录执行：

| 命令 | 用途 |
| --- | --- |
| `pnpm install` | 安装锁文件约束的依赖 |
| `pnpm dev` | 并行启动当前 Web 与 Server 开发进程 |
| `pnpm typecheck` | 全仓类型检查 |
| `pnpm test:unit` | 领域单元测试 |
| `pnpm test:integration` | Server 集成测试 |
| `pnpm test` | 全部 Vitest 测试 |
| `pnpm build` | 全仓生产构建 |
| `pnpm verify` | 类型、测试与构建总验证 |

## 2. 事实源与版本

- `README.md`：项目入口和文档导航，不承载需求规则。
- `redesign-v2/SPEC.md`：产品目标、功能、对象、流程、状态和验收。
- `redesign-v2/DESIGN.md`：共享视觉规范和唯一视觉入口；文件为空时不得从旧版推断设计。
- `redesign-v2/DESIGN-WEB.md`：桌面 Web 专属布局、组件、交互、动效和视觉验收。
- `redesign-v2/DESIGN-MINIAPP.md`：微信小程序专属布局、组件、交互、动效和视觉验收。
- `redesign-v2/design-reference/README.md`：可用参考图、确认层级、适用范围及原稿链接的唯一索引，不独立定义产品或视觉规则。
- `redesign-v2/prototype/`：当前原型；不存在时不得把旧原型称为当前实现。
- `TASK_LEDGER.md`：唯一当前任务台账，只记录当前目标、任务拆分、状态、阻塞、下一步、验收和证据，不复制产品、视觉或技术规则。
- `MEMORY.md`：与本文件同级的项目长期记忆，只保存稳定、可复用且有证据指针的结论，不记录当前进度。
- `WIKI_INDEX.md`：知识查询入口，索引结构化事实和必要的原始资料，不另建平行事实源。
- `SKILL.md`：固化用户偏好的写作、执行、判断、验收和落档方式，不保存当前任务状态或产品规则。
- `EVOLUTION.md`：可复用经验与方法，不是需求事实源；已删除旧版只保留在 Git 历史中，不得默认恢复或继承。

同一规则只保留一个当前事实源。产品行为以 `SPEC.md` 为准；共享视觉先读 `DESIGN.md`，再按目标端读取 `DESIGN-WEB.md` 或 `DESIGN-MINIAPP.md`；图片只按 `design-reference/README.md` 的确认层级和适用范围使用。会话、交接、memory、文件名和日期只能定位历史，不能覆盖当前事实源。

发生冲突时按以下优先级处理：用户最新明确要求 → 当前权威事实源 → `TASK_LEDGER.md` 当前项 → `MEMORY.md` / Wiki 摘要 → Compact、交接、聊天和工具记忆。低优先级内容不得静默覆盖高优先级内容；无法消解且会改变用户可见结果时暂停确认。

“完整视觉定稿”可以约束对应页面及明确标注的共享外壳；“已确认流程 / 结构”只约束流程和内容组织。缺失的共享视觉回到 `DESIGN.md`，端侧规则回到对应端侧规范。参考图或实现与事实源冲突时，先停止采用争议内容并完成对账。

进入技术设计后再创建 `redesign-v2/TECHNICAL.md`，集中记录架构、数据、接口、任务、存储和安全；技术工作开始前不创建空文档，也不从产品或视觉文档推断架构。

## 3. 工作边界、历史坑点与长期约定

### 事实优先硬规则（Fact-First）

- 所有判断以可验证事实为最高优先级；证据不足时必须明确标记为“未知 / 尚未确认 / 推测”，禁止为了给出完整答案而用合理猜测补全事实。
- 用户的认同、猜测或“应该是”不能替代事实校验，也不得把候选结论自动升级为已验证事实。
- 缩写、专有名词、版本号、Commit、规则名等关键标识，在确定表述前必须核对真实对应关系；无法核实时保持未知。
- 回答和执行时严格区分：已验证事实 / 有证据的推断 / 无证据未知。只有已验证事实可以使用确定语气。
- 新证据与既有回答或记录冲突时，以当前权威事实源和真实状态为准，并主动纠正旧结论；不得为了维持前后一致而维护错误。
- 涉及 Agent / Controller 身份、Session / Ownership、HEAD / Commit / Branch / Worktree、Git diff、TASK_LEDGER、Runtime、Skill / Rule / Handshake、Reviewer 结论和当前开发进度时，禁止仅凭聊天、Compact、记忆或历史印象作确定性判断；必须先读取对应当前真实源。
- 当“回答完整”与“事实准确”冲突时始终选择事实准确：宁可明确回答“目前无法确认”，也不得填补证据空白。

- 开始前确认仓库根目录、任务范围和未提交改动；现有改动默认属于用户，不覆盖、回退或整理无关内容。
- 只修改获批范围，优先最小可验证闭环；未经当前证据验证的行为、页面、外部调用或结果不得称为已完成。
- 完成相称验证并审查实际 diff 后，只要当前变更能与用户或其他工作树的现场清楚隔离，就必须自动创建范围单一的本地 Git 提交，不需要再次征求授权；无法隔离时不得提交，并把重叠与恢复点写入台账。合并、推送、PR、发布、部署、删除数据和其他难恢复操作仍分别遵守其授权边界。
- 用户已持续授权：SelfAlone 当前及后续凡已完成相称验证、范围单一且已顺序集成本地 `main` 的普通代码或文档提交，可由项目总控直接以非强制、快进方式推送到现有配置远端，无需逐次询问；推送前仍须核对真实 `main`、工作区、上游关系和实际提交边界，推送后回读远端 revision。该授权不含 force push、PR 合并、发布、部署、删除或生产数据变更；远端分叉、范围不明或结果未知时立即停止，不自动重试。
- 如使用子代理，`fork_turns` 使用 `"none"` 或正整数字符串；主 Agent 根据任务难度、时延和成本选择适合的 Agent 类型、模型和推理强度，可在派发时覆盖全局默认值。总控、复杂根因和高风险非作者审查使用能力优先配置，常规实现使用平衡配置，清晰窄范围工作使用高效配置；主 Agent 仍须审查实际 diff 并完成最终验收。
- SelfAlone 当前及后续后端开发、后端调试和后端非作者审查工作包默认使用 `provider=grok-build`、`model=grok-4.6`、`auth_mode=oauth`、`credential_source=现有 CLI session`；该会员通道在项目内无需逐包重复授权。每次仍须记录精确工作包、绝对 `cwd`、base、owned files、验收、实际 session / model / auth 元数据和结果。外部调用失败后不得自动重复可能计费或计额的同一路由；若属于 Adaptive Delivery 定义的安全失败，则统一进入宿主感知 fallback：先使用当前总控所在端的内部 GPT，同端明确额度 / 服务 / 模型不可用且本 attempt 已终止、无未知写入时，再使用另一端同档内部 GPT；跨端不得重置同一 execution lineage / recovery budget。路由变更不得成为重做、覆盖或并行写同一候选的理由。本授权不含前端 / UI、ImageGen、部署、push、生产数据变更、API Key 或其他付费服务，这些继续遵守各自授权门。
- SelfAlone 当前及后续 Web / 小程序前端开发、前端调试和前端代码审查工作包默认使用 `provider=kimi-code`、`model=kimi-k3`、`auth_mode=api`、`credential_source=os-keychain`；该 Kimi 开放平台按量付费通道在项目内无需逐包重复授权。每次仍须记录精确工作包、绝对 `cwd`、base、owned files、验收、实际 model / auth 元数据和结果。调用失败时不得自动重复可能计费的外部调用；结果未知或可能已有部分修改时必须 fail closed。只有 Adaptive Delivery 判定为安全失败时才自动降级：先使用当前总控宿主内部 GPT，同端明确额度 / 服务 / 模型不可用且 execution terminal 后，再由真实 peer-host adapter 切到另一端同档 GPT；没有真实 adapter 就 `peer_host_unavailable`，禁止用浏览器 UI 冒充子 Agent。已有 `ACTIVE / VERIFY / CANDIDATE` 不因路由变化重做或改派；只有新的工作包或重新形成的新 Assignment 才按本规则路由。本授权不含 UI / UX 产品决策、独立视觉验收、ImageGen、部署、push、生产数据变更或其他付费服务，这些继续遵守各自授权门。

以下坑点视为长期禁区：

- 不能把参考图、静态页面、测试数量、Agent 报告或共享数据库现状冒充当前分支的真实功能。
- 不能先搭功能骨架再把视觉规范和参考图留到以后；Web 与小程序分别在真实端侧完成视觉、状态和交互验收。
- Git worktree 只隔离文件，不隔离 PostgreSQL、端口、后台进程和产物目录；共享环境必须单独登记或隔离。
- 不从已删除旧版的 Git 历史、旧对话、文件名或日期静默继承产品和视觉规则。
- 不静默合并已有数据的账户，不把失败任务的清理连带到历史成功作品。

## 4. 任务读取与修改

- 新参与者或新会话先读 `README.md` 和 `TASK_LEDGER.md` 当前活动项，再按其中的证据与文件范围读取直接事实源；需要背景时先查 `WIKI_INDEX.md`，仍不足才追溯原始资料，不默认加载全部文档。
- 开始新工作包前除当前分支外，还要检查 `git worktree list`、未合并提交和各工作树状态；发现目标重叠时先把候选提交、依赖、未提交 diff 和验证缺口登记到台账，再决定复用、重做或废弃，禁止并行重复实现。只有已派发、需要隔离写入且文件边界明确的执行者才创建 worktree，Reviewer、占位和纯消息任务不得创建；候选合入或废弃后在同一事件链移除 clean worktree，脏现场先形成范围明确的提交或带原路径与 `HEAD` 的可恢复 stash / 分支再回收，不得把历史 worktree 长期留作档案。
- 本地 PostgreSQL、对象存储和外部服务状态可能被不同工作树共享，不能据此推断当前分支已经实现对应功能。迁移前先记录当前 schema、数据量和 owner 分布；并行开发默认使用隔离 schema 或独立实例。
- 产品、交互或原型任务读取 `SPEC.md`；实际视觉设计再读取 `DESIGN.md`、目标端规范和 `design-reference/README.md`，不得加载另一端规范后混用。
- 用户确认新的产品或视觉规则后，同步实际受影响的事实源、原型和测试；仅确认不改变规则的本地参考图时走快速通道。
- 搜索使用 `rg`，手工编辑使用 `apply_patch`。根目录只放项目入口和全局文件；素材、截图、输出和工具进入所属版本目录。

## 5. 上下文、Goal 与台账

- 长任务、台账粒度、Goal、Compact、Wiki、Raw Sources 和长期记忆的方法统一遵循全局 `adaptive-delivery` Skill；本项目文件不复制方法说明。
- `adaptive-delivery` 是 SelfAlone 唯一持续项目总控的运行合同，不是遇到台账问题才临时引用。项目总控保留范围、目标、动态拆分与调度、共享契约、文档职责与一致性、唯一台账、候选 / 分支集成、冲突处理和最终验收；实现、QA 与非作者审查按无写入重叠的边界分派。这里的“持续”指持续所有权与事件触发式履职，正常执行期间回到 idle，不以高频轮询或定时唤醒持续占用模型。不得按一个问题或一份文档机械增设长期管理 Agent；没有总控时由当前主 Agent 先接管并建立控制面。
- 子任务 ACK、候选提交、验收结论、失败、用户反馈或主线集成任一事件发生后，项目总控必须在同一事件链先核对真实主线 `HEAD`、状态和唯一台账，再同步受影响任务的状态、证据、阻塞与下一步；不得用控制工作树中的旧台账副本继续调度。新证据否定既有视觉结论时自动重开受影响的视觉门，旧 `PASS` 只保留未受影响的局部收据，不得继续代表整页或整个工作包通过。
- 项目总控必须从当前唯一主线加载适用的 `AGENTS.md` 与 Skill 合同；若总控运行在落后主线的隔离工作树，必须在每个事件回合开始时显式读取真实主线对应文件，并在调度收据记录已加载的主线提交。治理规则提交后，在总控回传“已加载精确提交”的 ACK 前不得宣称规则已经生效；长期落后的控制工作树不能继续作为调度事实源。
- 产品、视觉、技术、协作或验收规则提交后，变更者必须在同一事件链列出所有受影响的 live Agent / 任务，向其发送精确主线提交、变更摘要、受影响范围和新停止条件，并逐个取得“已加载精确提交”的 ACK；共享文件可见、消息已发送或台账已更新都不能替代加载确认。未 ACK 的 Agent 暂停受影响范围的下一次写入、冻结或验收，但无关工作继续；调度收据必须记录已通知 / 已 ACK / 待 ACK 清单，全部相关 ACK 闭合后才可宣称新规则已生效。
- 每次台账变化后重新计算 `READY`：所有文件边界、运行环境和依赖互不冲突的工作包应在可用容量内并行派发；某项等待 UI/UX、素材或非作者复验时，不阻塞其他已就绪工作。总控结束当前调度轮次前还必须清空自己当前即可执行的候选审查、顺序集成、验收 / Reviewer 派发、ACK 追问、恢复动作和已发生状态变化的台账同步；存在 `ACTIVE` Writer 不能替代这项检查。只有总控本人无立即动作、没有可执行 `READY`、没有待回收结果且真实外部等待或明确负责人已登记时，才可结束当前调度轮次。
- 每个调度轮次结束前必须形成可核验的调度收据：记录真实主线 `HEAD` 与台账提交、总控自己的 live Agent / 可用槽位、每个 `READY` 项的派发结果或精确未派发原因、已创建任务 ID 与 ACK 状态。Writer 接单后的第一条动作必须通过能实际送达总控的显式消息回传工作树 / 分支 / `HEAD` / 状态、精确文件边界、首个复现或 RED 和停止条件；只留在子任务自身 commentary 不算 delivered ACK。只有任务创建调用真实返回后才可标记 `DISPATCHED`，只有 delivered ACK 闭合后才可标记 `ACTIVE`。收据同时记录 dispatch 到 delivered ACK 的延迟、被误中断的 active writer 数和 clean-but-live 核对结果；`ACK PENDING`、只读审查或“已写入台账”均不代表开发已启动，也不能成为让无冲突空闲槽位闲置的理由。
- Writer 的交付只可标记 `CANDIDATE / READY FOR EARLY`；EARLY 修复冻结后只可标记 `READY FOR FINAL`。只有项目总控审查实际候选、顺序合入本地 `main`，完成真实业务 Case、视觉 / 状态复验并回写台账后，工作包才可标记 `DONE`。
- UI / UX writer 写入前必须回传绝对 `cwd`、branch、`HEAD / status`、精确 owned files、绑定参考 ID / SHA / 适用状态 / 视口 / 排除项和真实端自验计划，缺任一项不得写入。所有 writer 每次写入前和提交前都须重新校验绝对仓库根、branch、`HEAD / status`；隔离 writer 禁止写 `main`，发现越界立即停写并交总控对账，不得静默移动、覆盖或丢弃。
- 已有确认参考覆盖的页面、组件和状态必须忠实实现，不得重新设计。参考未覆盖的派生页面、派生组件或新增状态必须先由 UI / UX 角色依据 `DESIGN.md`、对应端规范和最接近的确认参考形成 `DESIGN-READY` 合同，明确结构、组件复用、Token、状态、交互、响应式、资产和验收视口；开发 Agent 在合同返回前不得写视觉实现。UI / UX 可以在用户既有视觉授权内直接选择最佳方案，但不得改变重大功能、信息架构或核心流程。
- 本项目自本规则生效后禁止新增 `::before`、`::after`、`:before`、`:after` 伪元素实现界面视觉；不得用伪元素、文本字符、Emoji 或临时 CSS 几何代替参考图中的图标、按钮、状态标记、分隔、装饰或背景。正式视觉使用真实 DOM / WXML 结构、成熟 SVG / 矢量图标或已确认透明资产。现存伪元素只作为待清理基线，Writer 触及其所属组件或页面时必须在同一包替换；提交前运行 `pnpm verify:visual-contract`，不得扩大基线。
- 总控采用短事件回合：一次只闭合一组相关的状态同步、派发或集成，形成收据后立即 yield，禁止把多个互不相关工作包长期塞在同一未结束 turn 中；但不能把已经具备输入的候选审查、集成、验收、追问或恢复动作仅写成“下一短事件”后收工，下一短事件只用于等待尚未发生的新输入。判定子任务 `ACK PENDING` 或中断前必须核对消息送达、最近子任务线程活动、工具事件和工作树四类证据；clean 只表示尚未写文件，不能证明未工作。第一次缺少 delivered ACK 时定向 follow-up 要求立即 checkpoint；第二次只有在四类证据都无新增时才中断并重建，若线程或工具仍有进展则要求收缩到 RED checkpoint，不得当作失联。单个 handshake 失效只回收对应 writer 并补派其他无冲突 `READY`，只有全部当前项均有精确阻塞且没有其他 `READY` 时才可阻塞 Goal；连续两次无新证据不得继续轮询。同一总控连续两次违反调度闭环时，从当前 main / 台账恢复点交接到新的唯一总控并停止旧总控，不增设第二总控。
- 视觉实现已有绑定参考和明确派生关系时直接忠实实现并做真实端对照，不得让开发 Agent 重新设计，也不为机械复刻或响应式修复浪费生图额度。缺少精确视觉方向的新增页面 / 重大组件，或同一视觉范围连续两次因主观设计偏差被否决时，开发写入前必须由 UI/UX 角色给出方案，并在已有授权额度内优先使用 ImageGen 生成精确候选。用户已授权 UI/UX 角色在不涉及重大功能、信息架构、核心流程或大范围返工的视觉范围内直接选择最佳候选，无需逐图等待确认；选择须记录比较依据、采用点与排除项。生成图在完成该选择和事实源登记前只作为 `CANDIDATE`，不得直接成为运行时截图、文字、功能图标或固定像素；须拆成可实现的结构、Token、状态、交互、响应式和验收合同后才能释放开发。缺失的山亭、纹理、插画等栅格资产可走 ImageGen；功能图标使用成熟 SVG / 矢量方式实现，但其轮廓、比例、线宽、圆角、留白、颜色及选中 / 按压状态必须忠实跟随已选 ImageGen 候选和绑定参考，不得以“使用图标库”为由换成另一套样式。
- UI 写入者首次形成可运行的 `HTML/CSS`、`WXML/WXSS` 或原生界面 diff 后必须立即停止扩写；交 EARLY 前须由 writer 自己打开真实 Chrome / 微信开发者工具，完成主视口同状态参考对照、关键交互、console / overflow 和状态恢复检查，明显偏差不得转交 reviewer，reviewer 不得成为第一个真正打开页面的人。随后提交同状态真实端截图给非作者 UI/UX 做 `EARLY-VISUAL-VERIFY`；未取得 EARLY verdict 时不得把大批功能与视觉一次性冻结后直接送 FINAL。测试绿灯、代码提交或结构行为正确不能替代 EARLY。
- EARLY `FAIL` 优先由原 writer 在原文件范围返工且不领取新包；同一根因连续两次失败必须重新诊断或拆小，禁止机械叠加补丁。
- 微信小程序真实端验收只保留一个由总控登记的常驻 DevTools 项目入口。隔离 writer 不得把各自 worktree 反复导入微信开发者工具；它们先完成互斥文件的范围单一 checkpoint，由集成者审查并顺序放入唯一小程序候选，再从同一 DevTools 路径执行 EARLY / FINAL。只有无法在唯一候选上保真复现且已登记精确原因时才允许一次性临时入口，并须在回合结束前移除最近入口但不删除源码或 worktree。
- 收到新需求时，项目总控先与产品、视觉、技术事实源、现有实现、成本、风险和返工面核对。存在内部或事实冲突、效率 / 质量下降、风险扩大或更优方案时，先给出证据、影响、选项和明确推荐；普通专业分歧按推荐继续推进，只有重大功能、信息架构、核心流程、大范围返工或授权边界需要用户裁决，不能以顺从代替判断。
- `TASK_LEDGER.md` 只保存当前目标、任务拆分、状态、阻塞、下一步、验收和证据；项目知识从 `WIKI_INDEX.md` 查询，仍不足时回溯 `raw_sources/`；稳定结论才写入 `MEMORY.md`。
- SelfAlone 的事实优先级、主线、授权和真实端验收边界仍以本文件其他章节及当前权威事实源为准；Compact 恢复后必须先用当前仓库、台账和事实源校验。

## 6. 参考图与轻量文档快速通道

- 普通文案、链接、索引小改，以及登记用户已明确认可、已在本地的单张参考图或同一确认批次，只要不改变产品行为、视觉规则、代码或原型，均由主 Agent 直接完成；不加载编排或构思技能，不分派子代理，不创建额外计划或设计文档。
- 文档小改只读取目标文件及直接事实源。参考图只核对用户确认、客户端、确认层级、适用范围、原稿或来源、尺寸与哈希；身份明确时不重复评审图片。
- 图片没有引入新规则时，只复制文件并更新 `design-reference/README.md`。不机械更新根 `README.md`、`TASK_LEDGER.md` 或 `EVOLUTION.md`。
- 最小验证为目标 diff、相关链接或文件元数据、尺寸与哈希以及 `git diff --check`。若来源、确认层级或范围不明，或与事实源冲突、扩展到实际设计和实现，则退出快速通道并先对账。

## 7. 项目质量与验收

- 手机按单任务、单列设计，不把桌面多栏机械缩窄。
- 数据界面区分加载、真实空、筛选空、失败和正常；失败保留输入、上下文和已完成结果，过期任务不得回写。
- 交互具备键盘焦点、可访问名称、非颜色状态提示、44×44px 触控目标，并尊重减少动态效果设置。
- 不新增生产依赖，不调用付费服务，不上传、发布或推送，除非用户明确授权；凭证不得进入聊天、源码、命令参数、日志或原型数据。
- 按风险运行定向测试、相关回归、链接检查、残留扫描和 `git diff --check`。原型须验证完整用户路径及相关视口；不以文件存在、静态选择器或 HTTP 成功代替真实体验。
- 最终说明完成内容、验证证据和真实限制；未实际验证的结果不得声称完成。
