# 老己移动端原生重构实施计划

1. 更新共享移动端骨架和书架：两列封面、带图标的底部导航、紧凑顶部操作。
2. 重构对话选书：用底部选择面板替代消息内横向封面列表，并保持现有 PPT 绑定数据流。
3. 收敛 PPT 手机端：把大纲与预览固定为全屏作品层，移动操作放到其自身顶部和底部。
4. 对齐本地书阅读：抽取 EPUB/PDF 共用的移动规则，保留正文优先、目录与划线想法按需呈现。
5. 精简设置页移动结构：紧凑资料行、原生分组列表、保持邮箱双验证。
6. 为每批补充结构断言，运行对应测试、全量原型测试、脚本语法和 HTML 链接检查。

## 实施状态（2026-08-10）

- [x] 共享移动端骨架与书架：底部导航图标、两列封面、紧凑导入入口。
- [x] 对话选书：消息内选书在手机端收敛为单一入口，底部面板使用两列封面。
- [x] PPT 作品层：大纲与预览在手机端进入全屏单任务层，返回后恢复对话。
- [x] EPUB/PDF 阅读：划线想法统一为底部面板，专注阅读控制保持一致。
- [x] 设置页：资料、连接服务和账户安全改为紧凑移动分组，保留邮箱双验证流程。

## 验证记录

- `node prototype/tests/*.test.js`：14 个原型测试全部通过。
- `node --check prototype/assets/laoji.js` 与 `node --check prototype/assets/laoji-state.js`：通过。
- `git diff --check`：通过。
- `laoji-prototype-links.test.js`：14 个页面链接目标通过。
- OD 设置页导出检查：`/private/tmp/laoji-settings-mobile-check.png` 已生成，桌面布局无重叠；移动断点由共享规则覆盖，断点结构断言已纳入 `laoji-mobile-native-redesign.test.js`。
