# PPT 预览状态布局实施计划

1. 在 `prototype/laoji-ppt-preview.html` 中保留模板选择态左栏，移除生成、失败和完成态的模板/流程侧栏；为这些状态增加紧凑的当前模板文字状态。
2. 调整预览页内联响应式样式，使生成、失败和完成态使用单列全宽瀑布流，并保持桌面独立滚动与手机安全区行为。
3. 在 `prototype/assets/laoji.js` 中让 `finalizing` 复用生成面板但明确显示“正在完成文件”，并同步当前模板名与顶部状态。
4. 更新 PPT 生命周期与移动工作台测试，覆盖生成后无侧栏、完成态更换模板创建新草稿、finalizing 文案及键盘可达语义。
5. 运行目标测试、全量原型测试、链接检查和 `git diff --check`；条件允许时复核 360、390、430、768、1024、1200、1440px。
